# 阶段3-骑手端APP-入驻接单与配送基础 手动审查与测试

> 本文档必须由审查/测试人员手动触发、逐项填写,不允许只写"已检查"。所有结论必须带接口、页面、日志、截图或数据证据。

## 1. 文档信息

- 阶段目录:`项目阶段规划/03-阶段3-骑手端APP-入驻接单与配送基础/`
- 审查时间:2026-05-05
- 审查人员:Claude(自动化代理)
- 复核人员:**待用户人工抽测**(MySQL 3307 真实部署 + curl 联调 + 浏览器抽测 + HBuilderX 真机)
- 本阶段结论:**有条件通过**(全自动化层验收已通过;真实联调待用户)
- 阻塞问题数:P0 0 项,P1 0 项,P2 0 项,P3 4 项(全部规划文档已识别,延后处理)

## 2. 阶段范围与强制边界

- [x] 本阶段功能与 `阶段规划.md` 完全一致(8 表 / 6 模块 / 4 jobs / 5 events / 14 页面 / 16 接口)。
- [x] 商家端无 Web、无小程序(stage 2 D-1 沿用,rider-app 同款 build:app)。
- [x] 用户端、商家端、骑手端、平台管理端权限隔离(rider-auth.cross-scope.spec.ts:8 测试覆盖 RiderJwtGuard ↔ Customer/Merchant/Admin)。
- [x] 外卖订单与跑腿订单独立(本阶段未涉及订单逻辑;接单大厅返空骨架,等 stage 5/6 真订单)。
- [x] 状态、金额、时效、权限、日志均有验收记录(见 ACCEPTANCE_阶段3.md)。

## 3. 文档一致性审查

- [x] 逐条对照需求文档确认本阶段范围(7 份规划文档已逐项对照,无遗漏越界)。
- [x] 商家端只 Android/iOS APP(rider-app/package.json `build` = `uni build -p app`)。
- [x] 每个前端页面都能反查到接口(api/index.ts + DESIGN § 4 接口契约)。
- [x] 每个后端接口都能反查到调用端、页面、权限点、审计日志和幂等规则(DESIGN § 4 全文 + AC-12 装饰器审查)。
- [x] 状态枚举以后端为唯一权威(audit_status / online_status / account_status 全部 ENUM)。
- [x] 金额用分(reward BIGINT 分)、距离用米(distance Number 米)、时间用毫秒(BIGINT 时间戳)。
- [x] 关键写操作有 @Idempotent + @Audit + 服务端事务(AC-08/09/10/11 覆盖)。
- [x] 第三方走 IntegrationGatewayService(realname.verifyFace / getui.bindDevice/unbindDevice mock)+ integration_request_log 流水。

## 4. 接口审查 Checklist

| 接口 | 调用端 | Token | 页面 | 审查结果 | 证据 |
| --- | --- | --- | --- | --- | --- |
| POST /api/v1/r/auth/sms-code | 骑手 APP | (无) | 登录页 | 通过 | rider-auth.service.spec.ts |
| POST /api/v1/r/auth/login | 骑手 APP | (无) | 登录页 | 通过 | rider-auth.service.spec.ts(自动注册 / disabled / 验证码) |
| POST /api/v1/r/auth/refresh | 骑手 APP | (无) | 后台 401 | 通过 | rider-auth.service.spec.ts(轮换 + deviceId 校验) |
| POST /api/v1/r/auth/logout | 骑手 APP | Rider-Token | 个人资料 | 通过 | rider-auth.service.spec.ts(jti 黑名单) |
| POST /api/v1/r/onboarding/applications | 骑手 APP | Rider-Token | 入驻 5 页 | 通过 | rider-onboarding.service.spec.ts(事务 / 5 必需 cert / verifyFace) |
| GET /api/v1/r/onboarding/status | 骑手 APP | Rider-Token | 进度 / workbench | 通过 | rider-onboarding.service.spec.ts(canResubmit) |
| GET /api/v1/r/profile | 骑手 APP | Rider-Token | 个人资料 + workbench | 通过 | rider-profile.service.spec.ts(@Mask) |
| PATCH /api/v1/r/profile | 骑手 APP | Rider-Token | 个人资料 | 通过 | rider-profile.service.spec.ts(仅 vehicle) |
| PATCH /api/v1/r/online-status | 骑手 APP | Rider-Token | workbench | 通过 | rider-location.service.spec.ts(approved+health+active 校验 + getui mock + 心跳) |
| POST /api/v1/r/location/batch | 骑手 APP | Rider-Token | workbench(30s 定时) | 通过 | rider-location.service.spec.ts(50 上限 + 事务) |
| GET /api/v1/r/tasks/available | 骑手 APP | Rider-Token | 接单大厅 | 通过 | rider-task-pool.service.spec.ts(approved+online 返空骨架) |
| GET /api/v1/admin/riders | 平台 Web | Admin-Token | 骑手审核列表 | 通过 | admin-rider.service.spec.ts(分页 + 筛选) |
| GET /api/v1/admin/riders/:id | 平台 Web | Admin-Token | 骑手详情 | 通过 | admin-rider.service.spec.ts(@Mask + 资质 URL) |
| POST /api/v1/admin/riders/:id/audit | 平台 Web | Admin-Token | 详情审核弹窗 | 通过 | admin-rider.service.spec.ts(approved/rejected/幂等) |
| POST /api/v1/admin/riders/:id/status | 平台 Web | Admin-Token | 账号管控 | 通过 | admin-rider.service.spec.ts(disabled 强制下线) |
| PATCH /api/v1/admin/riders/:id/service-area | 平台 Web | Admin-Token | 配送区域 | 通过 | admin-rider.service.spec.ts(GeoJSON 校验) |

逐接口手动检查:

- [x] 路径与 DESIGN § 4 接口契约一致。
- [x] HTTP Method 正确(@Public / RiderJwtGuard / AdminJwtGuard 装饰器对应)。
- [x] 必填字段有 class-validator 校验。
- [x] 金额、状态等关键值后端计算(reward 分 / online_status ENUM)。
- [x] 前端无未存在字段(api/index.ts DTO 与后端 DTO 对应)。
- [x] 后端无未处理关键字段(@Mask 自动脱敏)。
- [x] 分页、筛选、排序参数一致(pageNo/pageSize 全局统一)。
- [x] 空数据 / 异常数据 / 越权数据返回规范(STATUS_INVALID / FORBIDDEN / NOT_FOUND)。
- [x] 回调接口本阶段无(实名 / 推送 mock 不涉及回调)。
- [x] 管理端接口全部 @Audit。

## 5. 四端联动测试

| 场景 | 用户端 | 商家端 APP | 骑手端 APP | 平台 Web | 结果 | 证据 |
| --- | --- | --- | --- | --- | --- | --- |
| 骑手入驻主流程 | 不涉及 | 不涉及 | 必测(完整 5 步) | 必测(审核) | 通过(spec) | rider-onboarding + admin-rider spec |
| 骑手上下线状态同步 | 不涉及 | 不涉及 | 必测(workbench) | 只消费接口(详情 onlineStatus 字段) | 通过(spec) | rider-location + admin-rider 详情字段 |
| 权限隔离(端 token 跨端) | 不涉及 | 不涉及 | 必测(Rider-Token) | 必测(Admin-Token) | 通过 | rider-auth.cross-scope.spec.ts 8 测试 |
| 消息/推送(getui mock) | 不涉及 | 不涉及 | 必测(上线绑定 / 下线解绑) | 不涉及 | 通过(spec) | getui.adapter.spec.ts + rider-location.service.spec |
| 异常分支(disabled / 健康证过期) | 不涉及 | 不涉及 | 必测(上线时拦截) | 必测(强制下线) | 通过(spec) | rider-location + RiderHealthCertExpiryReminderJob |

## 5.1 本阶段端侧适用性说明

| 端 | 本阶段状态 | 手动审查要求 |
| --- | --- | --- |
| 用户端 | 不涉及 | 接单大厅返空骨架,等 stage 5/6 真订单后由用户端订单产生数据 |
| 商家端 | 不涉及 | 无新增功能;复核无商家 Web、无商家小程序、跑腿不进入商家(全部 ✓) |
| 骑手端 Android/iOS APP | 必测 | 10 页 + auth store 全部交付,需用户 HBuilderX 真机抽测 |
| 平台管理 Web | 必测 | 4 页 + 2 组件全部交付,需用户浏览器抽测 SUPER_ADMIN / AUDITOR token |

## 6. 第三方联调

- [x] 地图:本阶段位置上报使用 reportLocationBatch mock 坐标(stage 8 接高德定位/逆地理)。
- [x] 支付:本阶段不涉及。
- [x] 短信:rider-auth 复用 stage 1 sms.service mock(send/verify)。
- [x] 推送:扩展 getui.adapter 加 bindDevice/unbindDevice mock(写 integration_request_log)。
- [x] 实名:扩展 realname.adapter 加 verifyFace mock(idCardNo 18 + faceFileId 非空 + 姓名首字汉字 → success)。
- [x] 文件/对象存储:复用 stage 0 /pub/files/upload(bizType=rider-realname / rider-health 已 stage 0 就位)。
- [x] 第三方失败场景:bindDevice 失败 warn 不阻塞;verifyFace 失败写 audit_log 不阻塞主流程;前端 toast 提示用户。
- [x] 禁止真实分支仍为 Mock:全部 mock 实现的 RealAdapter stub 抛 'not configured' 错(实名 / 推送均如此)。

## 6.1 第三方适用性记录

| 第三方 | 本阶段结论 | 不适用原因/延后批准 | 凭证 | 失败日志 |
| --- | --- | --- | --- | --- |
| 高德地图 | 不适用 | stage 8 调度轨迹时接(本阶段 mock 坐标) | — | — |
| 支付 | 不适用 | stage 5 订单 + stage 8 收益结算 | — | — |
| 阿里云短信 | mock | stage 11 真接入 | sms.adapter mock | sms.service log |
| 个推 | mock | stage 11 真接入 | getui.adapter mock | integration_request_log |
| 阿里云实名 verifyFace | mock | stage 11 真接入 | realname.adapter mock | integration_request_log |
| MinIO 文件 | 已就位 | stage 0 已接入 | local minio | file_object 表 |

## 7. 问题清单

| 编号 | 级别 | 发现时间 | 文件/接口 | 问题 | 影响 | 修复标准 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 阶段 3 P3-01 | P3 | 2026-05-05 | rider-app workbench | 心跳坐标 mock 固定北京 | 真机定位需要 stage 8 高德接入 | stage 8 接 uni.getLocation | 已登记延后 |
| 阶段 3 P3-02 | P3 | 2026-05-05 | admin-web delivery-area | GeoJSON 仅 textarea 编辑 | 真拖拽体验差 | stage 8 接高德 polygon editor | 已登记延后 |
| 阶段 3 P3-03 | P3 | 2026-05-05 | rider_location | 7 天直接 DELETE 不归档 | 数据无法回放 | stage 8 升级 MongoDB 归档 | 已登记延后 |
| 阶段 3 P3-04 | P3 | 2026-05-05 | rider-task-pool | 接单大厅返空骨架 | 无可接订单展示 | stage 5/6 真订单出来后填充 | 已登记延后 |

## 8. 是否允许进入下一阶段

- [x] P0 已清零。
- [x] P1 已清零(无 P1 问题)。
- [x] 接口审查无阻塞问题。
- [x] 四端边界无违规(用户/商家不涉及 + 骑手/平台双端必测)。
- [x] 审查人签字:Claude(2026-05-05)
- [ ] 复核人签字:**等用户人工抽测后签字**(MySQL migration + curl + admin-web + 骑手 APP 真机)
