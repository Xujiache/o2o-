# 阶段2-商家端APP-入驻店铺与商品管理 接口契约清单

> 商家端仅 Android APP / iOS APP，禁止规划商家 Web 后台、商家小程序。外卖订单与跑腿订单必须独立订单体系、独立状态机、独立计价、独立退款规则。

## 全局接口规范

统一前缀：用户端 `/api/v1/c/**`，商家端 `/api/v1/m/**`，骑手端 `/api/v1/r/**`，平台 Web `/api/v1/admin/**`，公开 `/api/v1/pub/**`，第三方回调 `/api/v1/callback/**`。各端仅使用对应 Token：用户端只用 `Customer-Token`，商家 Android/iOS APP 只用 `Merchant-Token`，骑手 Android/iOS APP 只用 `Rider-Token`，平台 Web 只用 `Admin-Token`。

## 本阶段接口详情

### POST /api/v1/m/onboarding/applications

- 接口名称：提交商家入驻
- 调用端：商家 Android/iOS APP
- 鉴权 Token：Merchant-Token
- 权限点：`merchant:store:own`，商家只能访问和操作自己店铺的数据；商家端仅 Android/iOS APP 可调用。
- 使用页面/场景：入驻资料页
- 请求字段：licenseFileId、foodPermitFileId、legalPerson、idCardNo、storeName、businessScope、photos、deliveryArea
- 响应字段：applicationId、auditStatus、submittedAt
- 状态/枚举：以后端阶段状态机枚举为准，前端不得硬编码中文状态。
- 错误码：`INVALID_PARAM`、`UNAUTHORIZED`、`FORBIDDEN`、`DATA_NOT_FOUND`、`STATUS_INVALID`、`DUPLICATE_REQUEST`、`THIRD_PARTY_ERROR`
- 幂等要求：写操作必须支持 `Idempotency-Key`；重复请求返回同一业务结果，不重复扣款、不重复写业务变更。
- 审计日志：记录 traceId、operatorType、operatorId、targetType、targetId、beforeStatus、afterStatus、ip、deviceId、createdAt。
- 前端对接注意：页面必须处理 loading、空状态、错误提示、防重复点击和 token 过期跳转。

### GET /api/v1/m/onboarding/status

- 接口名称：查询入驻状态
- 调用端：商家 Android/iOS APP
- 鉴权 Token：Merchant-Token
- 权限点：`merchant:store:own`，商家只能访问和操作自己店铺的数据；商家端仅 Android/iOS APP 可调用。
- 使用页面/场景：审核进度页
- 请求字段：无
- 响应字段：auditStatus、rejectReason、canResubmit、lastSubmittedAt
- 状态/枚举：以后端阶段状态机枚举为准，前端不得硬编码中文状态。
- 错误码：`INVALID_PARAM`、`UNAUTHORIZED`、`FORBIDDEN`、`DATA_NOT_FOUND`、`STATUS_INVALID`、`DUPLICATE_REQUEST`、`THIRD_PARTY_ERROR`
- 幂等要求：写操作必须支持 `Idempotency-Key`；重复请求返回同一业务结果，不重复扣款、不重复写业务变更。
- 审计日志：记录 traceId、operatorType、operatorId、targetType、targetId、beforeStatus、afterStatus、ip、deviceId、createdAt。
- 前端对接注意：页面必须处理 loading、空状态、错误提示、防重复点击和 token 过期跳转。

### PATCH /api/v1/m/store/settings

- 接口名称：更新店铺设置
- 调用端：商家 Android/iOS APP
- 鉴权 Token：Merchant-Token
- 权限点：`merchant:store:own`，商家只能访问和操作自己店铺的数据；商家端仅 Android/iOS APP 可调用。
- 使用页面/场景：店铺设置页
- 请求字段：avatarFileId、name、intro、businessHours、deliveryArea、minOrderAmount、deliveryFee、notice
- 响应字段：storeId、updatedAt
- 状态/枚举：以后端阶段状态机枚举为准，前端不得硬编码中文状态。
- 错误码：`INVALID_PARAM`、`UNAUTHORIZED`、`FORBIDDEN`、`DATA_NOT_FOUND`、`STATUS_INVALID`、`DUPLICATE_REQUEST`、`THIRD_PARTY_ERROR`
- 幂等要求：写操作必须支持 `Idempotency-Key`；重复请求返回同一业务结果，不重复扣款、不重复写业务变更。
- 审计日志：记录 traceId、operatorType、operatorId、targetType、targetId、beforeStatus、afterStatus、ip、deviceId、createdAt。
- 前端对接注意：页面必须处理 loading、空状态、错误提示、防重复点击和 token 过期跳转。

### PATCH /api/v1/m/store/business-status

- 接口名称：切换营业状态
- 调用端：商家 Android/iOS APP
- 鉴权 Token：Merchant-Token
- 权限点：`merchant:store:own`，商家只能访问和操作自己店铺的数据；商家端仅 Android/iOS APP 可调用。
- 使用页面/场景：营业状态页
- 请求字段：businessStatus、reason
- 响应字段：storeId、businessStatus、effectiveAt
- 状态/枚举：以后端阶段状态机枚举为准，前端不得硬编码中文状态。
- 错误码：`INVALID_PARAM`、`UNAUTHORIZED`、`FORBIDDEN`、`DATA_NOT_FOUND`、`STATUS_INVALID`、`DUPLICATE_REQUEST`、`THIRD_PARTY_ERROR`
- 幂等要求：写操作必须支持 `Idempotency-Key`；重复请求返回同一业务结果，不重复扣款、不重复写业务变更。
- 审计日志：记录 traceId、operatorType、operatorId、targetType、targetId、beforeStatus、afterStatus、ip、deviceId、createdAt。
- 前端对接注意：页面必须处理 loading、空状态、错误提示、防重复点击和 token 过期跳转。

### POST /api/v1/m/products

- 接口名称：新增商品
- 调用端：商家 Android/iOS APP
- 鉴权 Token：Merchant-Token
- 权限点：`merchant:store:own`，商家只能访问和操作自己店铺的数据；商家端仅 Android/iOS APP 可调用。
- 使用页面/场景：商品编辑页
- 请求字段：categoryId、name、images、skus、price、stock、description、specs
- 响应字段：productId、status、createdAt
- 状态/枚举：以后端阶段状态机枚举为准，前端不得硬编码中文状态。
- 错误码：`INVALID_PARAM`、`UNAUTHORIZED`、`FORBIDDEN`、`DATA_NOT_FOUND`、`STATUS_INVALID`、`DUPLICATE_REQUEST`、`THIRD_PARTY_ERROR`
- 幂等要求：写操作必须支持 `Idempotency-Key`；重复请求返回同一业务结果，不重复扣款、不重复写业务变更。
- 审计日志：记录 traceId、operatorType、operatorId、targetType、targetId、beforeStatus、afterStatus、ip、deviceId、createdAt。
- 前端对接注意：页面必须处理 loading、空状态、错误提示、防重复点击和 token 过期跳转。

### PATCH /api/v1/m/products/{productId}/sale-status

- 接口名称：商品上下架
- 调用端：商家 Android/iOS APP
- 鉴权 Token：Merchant-Token
- 权限点：`merchant:store:own`，商家只能访问和操作自己店铺的数据；商家端仅 Android/iOS APP 可调用。
- 使用页面/场景：商品列表页
- 请求字段：saleStatus、reason
- 响应字段：productId、saleStatus、updatedAt
- 状态/枚举：以后端阶段状态机枚举为准，前端不得硬编码中文状态。
- 错误码：`INVALID_PARAM`、`UNAUTHORIZED`、`FORBIDDEN`、`DATA_NOT_FOUND`、`STATUS_INVALID`、`DUPLICATE_REQUEST`、`THIRD_PARTY_ERROR`
- 幂等要求：写操作必须支持 `Idempotency-Key`；重复请求返回同一业务结果，不重复扣款、不重复写业务变更。
- 审计日志：记录 traceId、operatorType、operatorId、targetType、targetId、beforeStatus、afterStatus、ip、deviceId、createdAt。
- 前端对接注意：页面必须处理 loading、空状态、错误提示、防重复点击和 token 过期跳转。

### POST /api/v1/admin/merchants/{applicationId}/audit

- 接口名称：平台商家审核
- 调用端：平台 Web
- 鉴权 Token：Admin-Token
- 权限点：`admin:merchants:manage`，平台 Web 必须校验管理员菜单权限、按钮权限和数据范围。
- 使用页面/场景：商家审核详情
- 请求字段：auditResult、rejectReason、commissionRate
- 响应字段：merchantId、storeId、auditStatus
- 状态/枚举：以后端阶段状态机枚举为准，前端不得硬编码中文状态。
- 错误码：`INVALID_PARAM`、`UNAUTHORIZED`、`FORBIDDEN`、`DATA_NOT_FOUND`、`STATUS_INVALID`、`DUPLICATE_REQUEST`、`THIRD_PARTY_ERROR`
- 幂等要求：写操作必须支持 `Idempotency-Key`；重复请求返回同一业务结果，不重复扣款、不重复写业务变更。
- 审计日志：记录 traceId、operatorType、operatorId、targetType、targetId、beforeStatus、afterStatus、ip、deviceId、createdAt。
- 前端对接注意：页面必须处理 loading、空状态、错误提示、防重复点击和 token 过期跳转。


## 接口审查清单

- [ ] 每个接口有调用端、Token、权限点。
- [ ] 每个接口有请求字段、响应字段、字段单位。
- [ ] 每个接口有错误码和前端展示策略。
- [ ] 每个写接口有幂等规则。
- [ ] 每个关键写接口有审计日志。
- [ ] 每个状态变更接口写明 beforeStatus 和 afterStatus。
- [ ] 每个接口能反查前端页面。
- [ ] 每个前端页面能反查接口。
- [ ] 第三方回调有验签、去重、防重放。
