# 阶段5-用户端-外卖交易闭环 接口契约清单

> 商家端仅 Android APP / iOS APP，禁止规划商家 Web 后台、商家小程序。外卖订单与跑腿订单必须独立订单体系、独立状态机、独立计价、独立退款规则。

## 全局接口规范

统一前缀：用户端 `/api/v1/c/**`，商家端 `/api/v1/m/**`，骑手端 `/api/v1/r/**`，平台 Web `/api/v1/admin/**`，公开 `/api/v1/pub/**`，第三方回调 `/api/v1/callback/**`。各端仅使用对应 Token：用户端只用 `Customer-Token`，商家 Android/iOS APP 只用 `Merchant-Token`，骑手 Android/iOS APP 只用 `Rider-Token`，平台 Web 只用 `Admin-Token`。

## 本阶段接口详情

### GET /api/v1/c/food/home

- 接口名称：外卖首页配置
- 调用端：用户端
- 鉴权 Token：Customer-Token 可选
- 权限点：`customer:self`，用户只能访问本人数据；订单、支付、售后等状态变更还需满足当前业务状态。
- 使用页面/场景：外卖首页
- 请求字段：cityCode、lng、lat
- 响应字段：banners、categories、activityEntries、recommendedStores
- 状态/枚举：以后端阶段状态机枚举为准，前端不得硬编码中文状态。
- 错误码：`INVALID_PARAM`、`UNAUTHORIZED`、`FORBIDDEN`、`DATA_NOT_FOUND`、`STATUS_INVALID`、`DUPLICATE_REQUEST`、`THIRD_PARTY_ERROR`
- 幂等要求：写操作必须支持 `Idempotency-Key`；重复请求返回同一业务结果，不重复扣款、不重复写业务变更。
- 审计日志：记录 traceId、operatorType、operatorId、targetType、targetId、beforeStatus、afterStatus、ip、deviceId、createdAt。
- 前端对接注意：页面必须处理 loading、空状态、错误提示、防重复点击和 token 过期跳转。

### GET /api/v1/c/food/stores

- 接口名称：商家列表
- 调用端：用户端
- 鉴权 Token：Customer-Token 可选
- 权限点：`customer:self`，用户只能访问本人数据；订单、支付、售后等状态变更还需满足当前业务状态。
- 使用页面/场景：外卖首页/搜索结果
- 请求字段：cityCode、keyword、categoryId、sort、lng、lat、pageNo、pageSize
- 响应字段：storeId、name、distance、sales、rating、deliveryFee、minOrderAmount、businessStatus
- 状态/枚举：以后端阶段状态机枚举为准，前端不得硬编码中文状态。
- 错误码：`INVALID_PARAM`、`UNAUTHORIZED`、`FORBIDDEN`、`DATA_NOT_FOUND`、`STATUS_INVALID`、`DUPLICATE_REQUEST`、`THIRD_PARTY_ERROR`
- 幂等要求：写操作必须支持 `Idempotency-Key`；重复请求返回同一业务结果，不重复扣款、不重复写业务变更。
- 审计日志：记录 traceId、operatorType、operatorId、targetType、targetId、beforeStatus、afterStatus、ip、deviceId、createdAt。
- 前端对接注意：页面必须处理 loading、空状态、错误提示、防重复点击和 token 过期跳转。

### GET /api/v1/c/food/stores/{storeId}/products

- 接口名称：商品列表
- 调用端：用户端
- 鉴权 Token：Customer-Token 可选
- 权限点：`customer:self`，用户只能访问本人数据；订单、支付、售后等状态变更还需满足当前业务状态。
- 使用页面/场景：店铺详情
- 请求字段：storeId
- 响应字段：categories、products、skus、stock、saleStatus、specs
- 状态/枚举：以后端阶段状态机枚举为准，前端不得硬编码中文状态。
- 错误码：`INVALID_PARAM`、`UNAUTHORIZED`、`FORBIDDEN`、`DATA_NOT_FOUND`、`STATUS_INVALID`、`DUPLICATE_REQUEST`、`THIRD_PARTY_ERROR`
- 幂等要求：写操作必须支持 `Idempotency-Key`；重复请求返回同一业务结果，不重复扣款、不重复写业务变更。
- 审计日志：记录 traceId、operatorType、operatorId、targetType、targetId、beforeStatus、afterStatus、ip、deviceId、createdAt。
- 前端对接注意：页面必须处理 loading、空状态、错误提示、防重复点击和 token 过期跳转。

### POST /api/v1/c/food/cart/items

- 接口名称：购物车变更
- 调用端：用户端
- 鉴权 Token：Customer-Token
- 权限点：`customer:self`，用户只能访问本人数据；订单、支付、售后等状态变更还需满足当前业务状态。
- 使用页面/场景：商品规格弹窗/购物车
- 请求字段：storeId、skuId、quantity
- 响应字段：cartId、items、goodsAmount、deliveryFee、discountAmount
- 状态/枚举：以后端阶段状态机枚举为准，前端不得硬编码中文状态。
- 错误码：`INVALID_PARAM`、`UNAUTHORIZED`、`FORBIDDEN`、`DATA_NOT_FOUND`、`STATUS_INVALID`、`DUPLICATE_REQUEST`、`THIRD_PARTY_ERROR`
- 幂等要求：写操作必须支持 `Idempotency-Key`；重复请求返回同一业务结果，不重复扣款、不重复写业务变更。
- 审计日志：记录 traceId、operatorType、operatorId、targetType、targetId、beforeStatus、afterStatus、ip、deviceId、createdAt。
- 前端对接注意：页面必须处理 loading、空状态、错误提示、防重复点击和 token 过期跳转。

### POST /api/v1/c/food/orders/preview

- 接口名称：订单试算
- 调用端：用户端
- 鉴权 Token：Customer-Token
- 权限点：`customer:self`，用户只能访问本人数据；订单、支付、售后等状态变更还需满足当前业务状态。
- 使用页面/场景：确认订单页
- 请求字段：storeId、items、addressId、couponId、pointsUsed、deliveryType、reservedTime
- 响应字段：goodsAmount、deliveryFee、discountAmount、payableAmount、estimatedDeliveryTime、unavailableReason
- 状态/枚举：以后端阶段状态机枚举为准，前端不得硬编码中文状态。
- 错误码：`INVALID_PARAM`、`UNAUTHORIZED`、`FORBIDDEN`、`DATA_NOT_FOUND`、`STATUS_INVALID`、`DUPLICATE_REQUEST`、`THIRD_PARTY_ERROR`
- 幂等要求：写操作必须支持 `Idempotency-Key`；重复请求返回同一业务结果，不重复扣款、不重复写业务变更。
- 审计日志：记录 traceId、operatorType、operatorId、targetType、targetId、beforeStatus、afterStatus、ip、deviceId、createdAt。
- 前端对接注意：页面必须处理 loading、空状态、错误提示、防重复点击和 token 过期跳转。

### POST /api/v1/c/food/orders

- 接口名称：提交外卖订单
- 调用端：用户端
- 鉴权 Token：Customer-Token
- 权限点：`customer:self`，用户只能访问本人数据；订单、支付、售后等状态变更还需满足当前业务状态。
- 使用页面/场景：确认订单页
- 请求字段：previewId、addressId、payChannel、remark、idempotencyKey
- 响应字段：orderId、orderNo、payOrderId、payableAmount、expireAt
- 状态/枚举：以后端阶段状态机枚举为准，前端不得硬编码中文状态。
- 错误码：`INVALID_PARAM`、`UNAUTHORIZED`、`FORBIDDEN`、`DATA_NOT_FOUND`、`STATUS_INVALID`、`DUPLICATE_REQUEST`、`THIRD_PARTY_ERROR`
- 幂等要求：写操作必须支持 `Idempotency-Key`；重复请求返回同一业务结果，不重复扣款、不重复写业务变更。
- 审计日志：记录 traceId、operatorType、operatorId、targetType、targetId、beforeStatus、afterStatus、ip、deviceId、createdAt。
- 前端对接注意：页面必须处理 loading、空状态、错误提示、防重复点击和 token 过期跳转。

### POST /api/v1/c/payments/prepay

- 接口名称：创建支付单
- 调用端：用户端
- 鉴权 Token：Customer-Token
- 权限点：`customer:self`，用户只能访问本人数据；订单、支付、售后等状态变更还需满足当前业务状态。
- 使用页面/场景：支付收银台
- 请求字段：bizType=FOOD、orderId、payChannel
- 响应字段：payOrderId、payParams、expireAt
- 状态/枚举：以后端阶段状态机枚举为准，前端不得硬编码中文状态。
- 错误码：`INVALID_PARAM`、`UNAUTHORIZED`、`FORBIDDEN`、`DATA_NOT_FOUND`、`STATUS_INVALID`、`DUPLICATE_REQUEST`、`THIRD_PARTY_ERROR`
- 幂等要求：写操作必须支持 `Idempotency-Key`；重复请求返回同一业务结果，不重复扣款、不重复写业务变更。
- 审计日志：记录 traceId、operatorType、operatorId、targetType、targetId、beforeStatus、afterStatus、ip、deviceId、createdAt。
- 前端对接注意：页面必须处理 loading、空状态、错误提示、防重复点击和 token 过期跳转。

### GET /api/v1/c/food/orders/{orderId}

- 接口名称：外卖订单详情
- 调用端：用户端
- 鉴权 Token：Customer-Token
- 权限点：`customer:self`，用户只能访问本人数据；订单、支付、售后等状态变更还需满足当前业务状态。
- 使用页面/场景：订单详情
- 请求字段：orderId
- 响应字段：orderStatus、payStatus、store、items、rider、timeline、actions
- 状态/枚举：以后端阶段状态机枚举为准，前端不得硬编码中文状态。
- 错误码：`INVALID_PARAM`、`UNAUTHORIZED`、`FORBIDDEN`、`DATA_NOT_FOUND`、`STATUS_INVALID`、`DUPLICATE_REQUEST`、`THIRD_PARTY_ERROR`
- 幂等要求：写操作必须支持 `Idempotency-Key`；重复请求返回同一业务结果，不重复扣款、不重复写业务变更。
- 审计日志：记录 traceId、operatorType、operatorId、targetType、targetId、beforeStatus、afterStatus、ip、deviceId、createdAt。
- 前端对接注意：页面必须处理 loading、空状态、错误提示、防重复点击和 token 过期跳转。

### GET /api/v1/c/food/orders/{orderId}/track

- 接口名称：订单轨迹
- 调用端：用户端
- 鉴权 Token：Customer-Token
- 权限点：`customer:self`，用户只能访问本人数据；订单、支付、售后等状态变更还需满足当前业务状态。
- 使用页面/场景：轨迹页
- 请求字段：orderId
- 响应字段：riderLocation、route、trackPoints、eta
- 状态/枚举：以后端阶段状态机枚举为准，前端不得硬编码中文状态。
- 错误码：`INVALID_PARAM`、`UNAUTHORIZED`、`FORBIDDEN`、`DATA_NOT_FOUND`、`STATUS_INVALID`、`DUPLICATE_REQUEST`、`THIRD_PARTY_ERROR`
- 幂等要求：写操作必须支持 `Idempotency-Key`；重复请求返回同一业务结果，不重复扣款、不重复写业务变更。
- 审计日志：记录 traceId、operatorType、operatorId、targetType、targetId、beforeStatus、afterStatus、ip、deviceId、createdAt。
- 前端对接注意：页面必须处理 loading、空状态、错误提示、防重复点击和 token 过期跳转。


## 接口审查清单

- [ ] 每个接口有调用端、Token、权限点。
- [ ] 每个接口有请求字段、响应字段、字段单位。
- [ ] 每个接口有错误码和前端展示策略。
- [ ] 每个写接口有幂等规则。
- [ ] 每个关键写接口有审计日志。
- [ ] 每个状态变更接口写明 beforeStatus 和 afterStatus。
- [ ] 每个接口能反查前端页面。
- [ ] 每个前端页面能反查接口。
- [ ] 第三方回调有验签、去重、防重放。
