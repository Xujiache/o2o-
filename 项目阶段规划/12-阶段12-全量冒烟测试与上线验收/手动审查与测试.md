# 阶段12-全量冒烟测试与上线验收 手动审查与测试

> 本文档必须由审查/测试人员手动触发、逐项填写，不允许只写“已检查”。所有结论必须带接口、页面、日志、截图或数据证据。

## 1. 文档信息

- 阶段目录：`项目阶段规划/12-阶段12-全量冒烟测试与上线验收/`
- 审查时间：
- 审查人员：
- 复核人员：
- 本阶段结论：通过 / 有条件通过 / 不通过
- 阻塞问题数：P0 __ 项，P1 __ 项，P2 __ 项，P3 __ 项

## 2. 阶段范围与强制边界

- [ ] 本阶段功能与 `阶段规划.md` 完全一致。
- [ ] 商家端仅 Android APP / iOS APP，无商家 Web、无商家小程序。
- [ ] 用户端、商家端、骑手端、平台管理端权限隔离。
- [ ] 外卖订单与跑腿订单独立建模、独立状态机、独立计价、独立退款。
- [ ] 本阶段涉及的状态、金额、时效、权限、日志均有验收记录。

## 3. 文档一致性审查

- [ ] 逐条对照需求文档确认本阶段范围，没有遗漏、越界或私自新增。
- [ ] 确认商家端只出现 Android APP / iOS APP，不出现商家 Web 或商家小程序。
- [ ] 确认每个前端页面都能反查到接口、字段、状态枚举、错误码和测试用例。
- [ ] 确认每个后端接口都能反查到调用端、页面、权限点、审计日志和幂等规则。
- [ ] 确认订单、支付、退款、售后、调度、提现等状态以后端枚举为唯一权威。
- [ ] 确认金额用分、距离用米、时间用毫秒时间戳或 ISO 标准且全端一致。
- [ ] 确认关键写操作有防重复提交、服务端幂等、审计日志、失败补偿。
- [ ] 确认第三方接口有验签、去重、重试、失败日志和用户可理解提示。

## 4. 接口审查 Checklist

| 接口 | 调用端 | Token | 页面 | 审查结果 | 证据/日志/截图 |
|---|---|---|---|---|---|
| POST /api/v1/admin/qa/smoke-records | 平台 Web/测试 | Admin-Token | 上线验收记录 | 待填写 | 待补证据 |
| GET /api/v1/admin/qa/interface-contract-report | 平台 Web/测试 | Admin-Token | 接口审查报告 | 待填写 | 待补证据 |
| GET /api/v1/admin/ops/release-gate | 平台 Web/运维 | Admin-Token | 上线门禁 | 待填写 | 待补证据 |

逐接口手动检查：

- [ ] 路径与 `接口契约清单.md` 一致。
- [ ] HTTP Method 正确。
- [ ] 必填字段有校验。
- [ ] 金额、库存、状态等关键值以后端计算为准。
- [ ] 前端没有使用不存在字段。
- [ ] 后端没有返回前端未处理的关键字段。
- [ ] 分页、排序、筛选参数一致。
- [ ] 空数据、异常数据、越权数据有返回规范。
- [ ] 回调接口支持签名校验、幂等、防重放。
- [ ] 管理端接口有操作日志。

## 5. 四端联动测试

| 场景 | 用户端 | 商家端 APP | 骑手端 APP | 平台 Web | 结果 | 证据 |
|---|---|---|---|---|---|---|
| 本阶段主流程 | 全量用户链路冒烟 | 商家端外卖订单接单/拒单/出餐/售后冒烟，不得出现跑腿处理入口 | 骑手接单/取货/配送/收益冒烟 | 后台审核/调度/售后/财务/日志冒烟 |  |  |
| 状态同步 | 按适用范围填写：必测/只消费接口/不涉及 | 按适用范围填写：必测/只消费接口/不涉及 | 按适用范围填写：必测/只消费接口/不涉及 | 按适用范围填写：必测/只消费接口/不涉及 |  |  |
| 权限隔离 | 按适用范围填写：必测/只消费接口/不涉及 | 按适用范围填写：必测/只消费接口/不涉及 | 按适用范围填写：必测/只消费接口/不涉及 | 按适用范围填写：必测/只消费接口/不涉及 |  |  |
| 消息/推送 | 按适用范围填写：必测/只消费接口/不涉及 | 按适用范围填写：必测/只消费接口/不涉及 | 按适用范围填写：必测/只消费接口/不涉及 | 按适用范围填写：必测/只消费接口/不涉及 |  |  |
| 异常分支 | 按适用范围填写：必测/只消费接口/不涉及 | 按适用范围填写：必测/只消费接口/不涉及 | 按适用范围填写：必测/只消费接口/不涉及 | 按适用范围填写：必测/只消费接口/不涉及 |  |  |

## 5.1 本阶段端侧适用性说明

| 端 | 本阶段状态 | 手动审查要求 |
|---|---|---|
| 用户端小程序 / Android APP / iOS APP | 按 `按端实施范围.md` 填写：必测 / 只消费接口 / 不涉及 | 不涉及时必须写“不涉及”，不得默认填“待测”。 |
| 商家端 Android/iOS APP | 按 `按端实施范围.md` 填写：必测 / 只消费接口 / 不涉及 | 必须复核无商家 Web、无商家小程序、跑腿不进入商家处理。 |
| 骑手端 Android/iOS APP | 按 `按端实施范围.md` 填写：必测 / 只消费接口 / 不涉及 | 不涉及时只做 Token/权限边界检查。 |
| 平台管理 Web | 按 `按端实施范围.md` 填写：必测 / 只消费接口 / 不涉及 | 必须复核后台权限、脱敏、审计日志。 |

## 6. 第三方联调

- [ ] 地图：定位、逆地理编码、距离计算、路线规划、轨迹回放。
- [ ] 支付：微信支付、支付宝支付、退款、关闭订单、对账。
- [ ] 短信：验证码、订单通知、售后通知、提现通知。
- [ ] 推送：APP 推送、小程序模板消息、后台站内消息。
- [ ] 实名：用户、骑手、商家法人认证。
- [ ] 文件/对象存储：证照、商品图、凭证上传与访问权限。
- [ ] 所有第三方失败场景有降级、重试、日志和用户提示。
- [ ] 禁止真实分支仍为 Mock、TODO、空实现。

## 6.1 第三方适用性记录

| 第三方 | 本阶段结论 | 不适用原因/延后批准人/复测计划 | 账号/环境 | 回调地址 | 验签/去重证据 | 失败重试日志 |
|---|---|---|---|---|---|---|
| 地图 | 必测 / 不适用 / 延后 |  |  |  |  |  |
| 支付 | 必测 / 不适用 / 延后 |  |  |  |  |  |
| 短信 | 必测 / 不适用 / 延后 |  |  |  |  |  |
| 推送 | 必测 / 不适用 / 延后 |  |  |  |  |  |
| 实名 | 必测 / 不适用 / 延后 |  |  |  |  |  |
| 文件存储 | 必测 / 不适用 / 延后 |  |  |  |  |  |

## 7. 问题清单

| 编号 | 级别 | 发现时间 | 文件/接口 | 问题 | 影响 | 修复标准 | 证据链接 | 状态 | 责任人 | 复测时间 | 关闭人 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 1 | P0 |  |  |  |  |  |  | 未修 / 已修 / 已复测 |  |  |  |
| 2 | P1 |  |  |  |  |  |  | 未修 / 已修 / 已复测 |  |  |  |
| 3 | P2 |  |  |  |  |  |  | 未修 / 已修 / 已复测 |  |  |  |
| 4 | P3 |  |  |  |  |  |  | 未修 / 已修 / 已复测 |  |  |  |

## 8. 是否允许上线/发布

- [ ] P0 已清零。
- [ ] P1 已清零或有明确延期批准。
- [ ] 接口审查无阻塞问题。
- [ ] 四端边界无违规。
- [ ] 全量冒烟测试通过率满足上线门禁。
- [ ] 回滚预案、监控告警、生产巡检责任人已确认。
- [ ] 发布签字已完成。
- [ ] 审查人签字：
- [ ] 复核人签字：
