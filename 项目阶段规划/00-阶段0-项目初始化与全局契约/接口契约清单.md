# 阶段0-项目初始化与全局契约 接口契约清单

> 商家端仅 Android APP / iOS APP，禁止规划商家 Web 后台、商家小程序。外卖订单与跑腿订单必须独立订单体系、独立状态机、独立计价、独立退款规则。

## 全局接口规范

统一前缀：用户端 `/api/v1/c/**`，商家端 `/api/v1/m/**`，骑手端 `/api/v1/r/**`，平台 Web `/api/v1/admin/**`，公开 `/api/v1/pub/**`，第三方回调 `/api/v1/callback/**`。各端仅使用对应 Token：用户端只用 `Customer-Token`，商家 Android/iOS APP 只用 `Merchant-Token`，骑手 Android/iOS APP 只用 `Rider-Token`，平台 Web 只用 `Admin-Token`。

## 本阶段接口详情

### GET /api/v1/pub/dictionaries

- 接口名称：获取字典枚举
- 调用端：用户端小程序 / Android APP / iOS APP、商家 Android/iOS APP、骑手 Android/iOS APP、平台 Web
- 鉴权 Token：公开只读接口无 Token；若登录后调用，仅允许使用当前端对应 Token。
- 权限点：`public:read`；如登录后调用，只能使用当前端对应 Token，不允许跨端混用。
- 使用页面/场景：启动初始化、状态标签组件
- 请求字段：typeList，可选
- 响应字段：dictType、code、label、sort、enabled
- 状态/枚举：以后端阶段状态机枚举为准，前端不得硬编码中文状态。
- 错误码：`INVALID_PARAM`、`UNAUTHORIZED`、`FORBIDDEN`、`DATA_NOT_FOUND`、`STATUS_INVALID`、`DUPLICATE_REQUEST`、`THIRD_PARTY_ERROR`
- 幂等要求：写操作必须支持 `Idempotency-Key`；重复请求返回同一业务结果，不重复扣款、不重复写业务变更。
- 审计日志：记录 traceId、operatorType、operatorId、targetType、targetId、beforeStatus、afterStatus、ip、deviceId、createdAt。
- 前端对接注意：页面必须处理 loading、空状态、错误提示、防重复点击和 token 过期跳转。

### GET /api/v1/pub/cities

- 接口名称：获取城市站点
- 调用端：用户端/骑手端/平台 Web
- 鉴权 Token：公开城市接口无 Token；需要个性化服务区域时仅允许使用当前端对应 Token。
- 权限点：`public:read`；如登录后调用，只能使用当前端对应 Token，不允许跨端混用。
- 使用页面/场景：城市选择、配送区域、后台城市配置
- 请求字段：keyword、enabled
- 响应字段：cityCode、cityName、province、serviceEnabled
- 状态/枚举：以后端阶段状态机枚举为准，前端不得硬编码中文状态。
- 错误码：`INVALID_PARAM`、`UNAUTHORIZED`、`FORBIDDEN`、`DATA_NOT_FOUND`、`STATUS_INVALID`、`DUPLICATE_REQUEST`、`THIRD_PARTY_ERROR`
- 幂等要求：写操作必须支持 `Idempotency-Key`；重复请求返回同一业务结果，不重复扣款、不重复写业务变更。
- 审计日志：记录 traceId、operatorType、operatorId、targetType、targetId、beforeStatus、afterStatus、ip、deviceId、createdAt。
- 前端对接注意：页面必须处理 loading、空状态、错误提示、防重复点击和 token 过期跳转。

### POST /api/v1/pub/files/upload

- 接口名称：文件上传
- 调用端：用户端小程序 / Android APP / iOS APP、商家 Android/iOS APP、骑手 Android/iOS APP、平台 Web
- 鉴权 Token：Customer-Token / Merchant-Token / Rider-Token / Admin-Token，按上传业务归属选择且不可混用。
- 权限点：`principal:file:upload`，按 Customer-Token / Merchant-Token / Rider-Token / Admin-Token 的当前主体归属校验上传业务类型。
- 使用页面/场景：资质、商品图、售后凭证、头像
- 请求字段：file、bizType、contentType
- 响应字段：fileId、url、expireAt、size
- 状态/枚举：以后端阶段状态机枚举为准，前端不得硬编码中文状态。
- 错误码：`INVALID_PARAM`、`UNAUTHORIZED`、`FORBIDDEN`、`DATA_NOT_FOUND`、`STATUS_INVALID`、`DUPLICATE_REQUEST`、`THIRD_PARTY_ERROR`
- 幂等要求：写操作必须支持 `Idempotency-Key`；重复请求返回同一业务结果，不重复扣款、不重复写业务变更。
- 审计日志：记录 traceId、operatorType、operatorId、targetType、targetId、beforeStatus、afterStatus、ip、deviceId、createdAt。
- 前端对接注意：页面必须处理 loading、空状态、错误提示、防重复点击和 token 过期跳转。

### GET /api/v1/admin/integrations/health

- 接口名称：第三方配置健康检查
- 调用端：平台 Web
- 鉴权 Token：Admin-Token
- 权限点：`admin:integrations:view`，平台 Web 必须校验管理员菜单权限、按钮权限和数据范围。
- 使用页面/场景：第三方配置页
- 请求字段：provider，可选
- 响应字段：provider、status、lastCheckedAt、errorMessage
- 状态/枚举：以后端阶段状态机枚举为准，前端不得硬编码中文状态。
- 错误码：`INVALID_PARAM`、`UNAUTHORIZED`、`FORBIDDEN`、`DATA_NOT_FOUND`、`STATUS_INVALID`、`DUPLICATE_REQUEST`、`THIRD_PARTY_ERROR`
- 幂等要求：写操作必须支持 `Idempotency-Key`；重复请求返回同一业务结果，不重复扣款、不重复写业务变更。
- 审计日志：记录 traceId、operatorType、operatorId、targetType、targetId、beforeStatus、afterStatus、ip、deviceId、createdAt。
- 前端对接注意：页面必须处理 loading、空状态、错误提示、防重复点击和 token 过期跳转。

### GET /api/v1/admin/audit-logs

- 接口名称：审计日志查询
- 调用端：平台 Web
- 鉴权 Token：Admin-Token
- 权限点：`admin:audit:logs:view`，平台 Web 必须校验管理员菜单权限、按钮权限和数据范围。
- 使用页面/场景：操作日志页
- 请求字段：operatorType、targetType、targetId、timeRange、pageNo、pageSize
- 响应字段：分页日志列表
- 状态/枚举：以后端阶段状态机枚举为准，前端不得硬编码中文状态。
- 错误码：`INVALID_PARAM`、`UNAUTHORIZED`、`FORBIDDEN`、`DATA_NOT_FOUND`、`STATUS_INVALID`、`DUPLICATE_REQUEST`、`THIRD_PARTY_ERROR`
- 幂等要求：写操作必须支持 `Idempotency-Key`；重复请求返回同一业务结果，不重复扣款、不重复写业务变更。
- 审计日志：记录 traceId、operatorType、operatorId、targetType、targetId、beforeStatus、afterStatus、ip、deviceId、createdAt。
- 前端对接注意：页面必须处理 loading、空状态、错误提示、防重复点击和 token 过期跳转。


## 接口审查清单

- [ ] 每个接口有调用端、Token、权限点。
- [ ] 每个接口有请求字段、响应字段、字段单位。
- [ ] 每个接口有错误码和前端展示策略。
- [ ] 每个写接口有幂等规则。
- [ ] 每个关键写接口有审计日志。
- [ ] 每个状态变更接口写明 beforeStatus 和 afterStatus。
- [ ] 每个接口能反查前端页面。
- [ ] 每个前端页面能反查接口。
- [ ] 第三方回调有验签、去重、防重放。
