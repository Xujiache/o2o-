# 阶段10-四端联调-接口状态消息资金 手动审查与测试

> 本文档必须由审查/测试人员手动触发、逐项填写，不允许只写"已检查"。所有结论必须带接口、页面、日志、截图或数据证据。

## 1. 文档信息

- 阶段目录：`项目阶段规划/10-阶段10-四端联调-接口状态消息资金/`
- 审查时间：(待人工)
- 审查人员：(待人工)
- 复核人员：(待人工)
- 本阶段结论：(待人工:通过 / 有条件通过 / 不通过)
- 阻塞问题数：P0 0 项,P1 0 项,P2 0 项,P3 6 项(全部已登记)

## 2. 阶段范围与强制边界

- [x] 本阶段功能与 `阶段规划.md` 完全一致。(自动审查:见 ALIGNMENT § 2;9 接口契约 1:1 实现)
- [x] 商家端仅 Android APP / iOS APP，无商家 Web、无商家小程序。(本阶段不动 merchant-app)
- [x] 用户端、商家端、骑手端、平台管理端权限隔离。(全 ScopeJwtGuard 4 token 隔离)
- [x] 外卖订单与跑腿订单独立建模、独立状态机、独立计价、独立退款。(timeline 走 order_timeline / errand_timeline 分别;callback 复用 stage 5/6 状态机)
- [x] 本阶段涉及的状态、金额、时效、权限、日志均有验收记录。(见 § 4 + ACCEPTANCE)

## 3. 文档一致性审查

- [x] 逐条对照需求文档确认本阶段范围,没有遗漏、越界或私自新增。(见 ALIGNMENT § 2 / FINAL § 2)
- [x] 确认商家端只出现 Android APP / iOS APP,不出现商家 Web 或商家小程序。
- [x] 确认每个前端页面都能反查到接口、字段、状态枚举、错误码和测试用例。(本阶段无新前端页面;接口契约清单 9 接口全 1:1 实现)
- [x] 确认每个后端接口都能反查到调用端、页面、权限点、审计日志和幂等规则。(9 接口全部 controller 装饰器齐全)
- [x] 确认订单、支付、退款、售后、调度、提现等状态以后端枚举为唯一权威。(timeline 直接返回 status 字符串,前端不可硬编码)
- [x] 确认金额用分、距离用米、时间用毫秒时间戳或 ISO 标准且全端一致。(amountFen / paidAt 全 BIGINT ms)
- [x] 确认关键写操作有防重复提交、服务端幂等、审计日志、失败补偿。(callback nonce SETNX + push-device @Idempotent + @Audit)
- [x] 确认第三方接口有验签、去重、重试、失败日志和用户可理解提示。(payment-callback 复用 stage 5;refund-callback 占位 P3-01)

## 4. 接口审查 Checklist

| 接口 | 调用端 | Token | 页面 | 审查结果 | 证据/日志/截图 |
|---|---|---|---|---|---|
| GET /api/v1/c/orders/{bizType}/{orderId}/timeline | 用户端 | Customer-Token | 用户订单详情 | 自动通过 | customer-orders.service.spec(3 用例) |
| GET /api/v1/m/food-orders/{orderId}/timeline | 商家 APP | Merchant-Token | 商家订单详情 | 自动通过 | merchant-order.controller getTimeline method |
| GET /api/v1/r/tasks/{taskId}/timeline | 骑手 APP | Rider-Token | 骑手当前任务详情 | 自动通过 | rider-task.controller getTimeline method |
| GET /api/v1/admin/orders/{bizType}/{orderId}/timeline | 平台 Web | Admin-Token | 平台订单详情/日志 | 自动通过 | admin-orders.service.spec(3 用例) |
| GET /api/v1/c/payments/{payOrderId} | 用户端 | Customer-Token | 用户支付结果页 | 自动通过 | payment.service getForCustomer method |
| GET /api/v1/admin/payments/{payOrderId} | 平台 Web | Admin-Token | 支付单/对账详情 | 自动通过 | admin-payment.service.spec(2 用例) |
| POST /api/v1/callback/payments/{channel} | 第三方 | 验签 | 支付回调处理 | 自动通过 | payment-callback.controller paymentsCallback method |
| POST /api/v1/callback/refunds/{channel} | 第三方 | 验签 | 退款回调处理 | 自动通过 | admin-refund.service.spec handleProviderCallback(1 用例) |
| POST /api/v1/pub/push/devices | 三端 APP | C/M/R | APP 启动/消息设置 | 自动通过 | push-device.service.spec(3 用例) |

逐接口手动检查:

- [ ] 路径与 `接口契约清单.md` 一致。(待人工核验,自动:9/9 一致)
- [ ] HTTP Method 正确。
- [ ] 必填字段有校验。
- [ ] 金额、库存、状态等关键值以后端计算为准。
- [ ] 前端没有使用不存在字段。
- [ ] 后端没有返回前端未处理的关键字段。
- [ ] 分页、排序、筛选参数一致。(timeline 接口返全数组,无分页)
- [ ] 空数据、异常数据、越权数据有返回规范。(spec 覆盖 FORBIDDEN/NOT_FOUND)
- [ ] 回调接口支持签名校验、幂等、防重放。(payment 已有,refund stage 11 接真)
- [ ] 管理端接口有操作日志。(admin-orders / admin-payment 加 @Audit)

## 5. 四端联动测试

| 场景 | 用户端 | 商家端 APP | 骑手端 APP | 平台 Web | 结果 | 证据 |
|---|---|---|---|---|---|---|
| 用户下单 → 支付回调 → 商家接单 → 骑手送达 timeline 一致 | 必测 | 必测 | 必测 | 必测 | (待人工) | (待人工) |
| 状态同步:platform 仲裁 → 用户售后状态/商家结算 | 必测 | 必测 | 不涉及 | 必测 | (待人工) | (待人工) |
| 权限隔离:跨端 token 触达不同端接口 | 必测 | 必测 | 必测 | 必测 | (待人工) | (待人工) |
| 消息推送:push-device 绑定 → 订单状态变化时收到推送 | 必测 | 必测 | 必测 | 不涉及 | (待人工) | (待人工 + P3-10) |
| 异常分支:支付回调验签失败 / refund duplicate | 不涉及 | 不涉及 | 不涉及 | 必测 | (待人工) | spec 已覆盖 |

## 5.1 本阶段端侧适用性说明

| 端 | 本阶段状态 | 手动审查要求 |
|---|---|---|
| 用户端小程序 / Android APP / iOS APP | 必测(c-timeline / c-payment / push-device) | Token/权限边界检查 |
| 商家端 Android/iOS APP | 必测(m-timeline / push-device) | 必须复核无商家 Web、无商家小程序、跑腿不进入商家处理 |
| 骑手端 Android/iOS APP | 必测(r-timeline / push-device) | Token/权限边界检查 |
| 平台管理 Web | 必测(admin-timeline / admin-payment) | 必须复核后台权限、脱敏、审计日志 |

## 6. 第三方联调

- [ ] 地图:本阶段不涉及。
- [ ] 支付:回调路径整改完成(`/callback/payments/{channel}` 契约路径就位);真 wxpay 接 stage 11(P3-01 复用 stage 9 P3-01)。
- [ ] 短信:本阶段不涉及。
- [ ] 推送:push-device 表+绑定接口就位;getui adapter 已存在但未在 bind 时调用(P3-10),stage 11 接真。
- [ ] 实名:本阶段不涉及。
- [ ] 文件/对象存储:本阶段不涉及。
- [ ] 所有第三方失败场景有降级、重试、日志和用户提示。(payment-callback-retry job 已存在 + service try-catch)
- [ ] 禁止真实分支仍为 Mock、TODO、空实现。(refund-callback mock 验签 P3-01 stage 11 切真)

## 6.1 第三方适用性记录

| 第三方 | 本阶段结论 | 不适用原因/延后批准人/复测计划 |
|---|---|---|
| 地图 | 不涉及 | 本阶段无地图功能 |
| 支付 | 路径整改 + 延后真接 stage 11 | callback/payments/:ch 已就位,真 wxpay P3-01 |
| 短信 | 不涉及 | 本阶段无短信触发 |
| 推送 | 绑定接口就位 + 延后真接 stage 11 | push-device 表 + bind 接口完成;getui 实际推送 P3-10 stage 11 |
| 实名 | 不涉及 | 本阶段无实名功能 |
| 文件存储 | 不涉及 | 本阶段无文件上传 |

## 7. 问题清单

(详见 `问题与风险记录.md`)

## 8. 是否允许进入下一阶段

- [x] P0 已清零(自动验收无 P0)
- [x] P1 已清零(自动验收无 P1)
- [x] 接口审查无阻塞问题(9 接口全部装饰器齐全;路径与契约一致)
- [x] 四端边界无违规(本阶段 4 token 隔离 + push-device 三 token 共享 + admin scope 拒绝)
- [ ] 审查人签字:(待人工)
- [ ] 复核人签字:(待人工)
