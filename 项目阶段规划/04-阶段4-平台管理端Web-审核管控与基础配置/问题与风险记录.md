# 阶段4-平台管理端Web-审核管控与基础配置 问题与风险记录

> 商家端仅 Android APP / iOS APP，禁止规划商家 Web 后台、商家小程序。外卖订单与跑腿订单必须独立订单体系、独立状态机、独立计价、独立退款规则。

## P0 阻塞级

- [x] **F-01 admin-user controller 路由顺序冲突**(已修复 2026-05-05)
  - 文件:`apps/server/src/modules/admin-user/admin-user.controller.ts`
  - 问题:`@Get(':id')` 注册在 `@Get('disable-records')` 之前,NestJS 按声明顺序匹配,`/admin/customers/disable-records` 会先命中 `:id`(id='disable-records'),进入 `getCustomerDetail` 路径报错
  - 影响:disable-records 页面接口 404 / 数据错乱
  - 修复:将 `@Get('disable-records')` 提到 `@Get(':id')` 之前,确保具体路径段先注册
  - 复测:server jest 53 suites 全绿,admin-user.service.spec disable-records 用例覆盖
  - 责任人:Claude

## P1 严重级

- [x] **F-02 admin-auth.ts 缺 refresh 调用函数**(已修复 2026-05-05)
  - 文件:`apps/admin-web/src/api/admin-auth.ts`
  - 问题:仅定义 Refresh endpoint 常量,无 refresh() 调用函数,导致 401 自动刷新无法实现
  - 修复:补 RefreshReq / RefreshVo 类型 + refresh() 函数(skipAuthRefresh:true 防递归)
  - 复测:typecheck 通过,F-03 单飞回放可调用

- [x] **F-03 utils/request.ts 401 不自动 refresh**(已修复 2026-05-05)
  - 文件:`apps/admin-web/src/utils/request.ts`
  - 问题:HTTP 401 直接 clearAuth + 跳登录,未尝试用 refreshToken 换新 token
  - 修复:实现 ensureRefreshing 单飞模式 + retry 标记,401 → refresh → 重发原请求;refresh 失败再清空跳登录
  - 复测:admin-web vitest 20 suites 全绿(login spec 含 401 mock)

- [x] **F-04 login 页不显示 lastLoginAt**(已修复 2026-05-05)
  - 文件:`apps/admin-web/src/views/login/index.vue`、`apps/admin-web/src/layout/index.vue`
  - 问题:DESIGN § 9.1 要求显示 lastLoginAt,但成功 toast 仅显示 displayName,layout 也无显示
  - 修复:成功 toast 加"上次登录 xxx",layout header 加 computed lastLoginText
  - 复测:login.spec 4 用例全绿

- [x] **F-05 login 页 ACCOUNT_LOCKED 无 modal 提示**(已修复 2026-05-05)
  - 文件:`apps/admin-web/src/views/login/index.vue`
  - 问题:DESIGN § 9.1 要求账号锁定弹 modal,实际所有错误统一 ElMessage.error
  - 修复:STATUS_INVALID 错误码 → ElMessageBox.alert(锁定/禁用差异化文案)
  - 复测:login.spec / typecheck 通过

## P2 重要级

- [ ] 暂无。

## P3 优化级

- [ ] **P3-01 admin-web cities 页 GeoJSON 仅 textarea**:真拖拽体验差,stage 8 接高德 polygon editor。
- [ ] **P3-02 RoleChanged 仅写 admin:role-revoked 标记,未真撤销 jti**:老 token 仍可用直至自然过期,stage 5+ 接 access token iat 比对。
- [ ] **P3-03 third-party reloadConfig 仅事件发布,无真热加载**:改 secret 后需重启服务才生效,stage 8 IntegrationGateway 真热更新。

## 待确认接口问题

| 接口 | 问题 | 阻塞级别 | 决策人 | 结论 |
|---|---|---|---|---|
|  |  |  |  |  |

## 待确认页面问题

| 端 | 页面 | 问题 | 阻塞级别 | 结论 |
|---|---|---|---|---|
|  |  |  |  |  |
