# 阶段4-平台管理端Web-审核管控与基础配置 手动审查与测试

> 本文档必须由审查/测试人员手动触发、逐项填写,不允许只写"已检查"。所有结论必须带接口、页面、日志、截图或数据证据。

## 1. 文档信息

- 阶段目录:`项目阶段规划/04-阶段4-平台管理端Web-审核管控与基础配置/`
- 审查时间:2026-05-05
- 审查人员:Claude(自动化代理)
- 复核人员:**待用户人工抽测**(MySQL 3307 真实部署 + curl 联调 + 浏览器抽测 admin-web)
- 本阶段结论:**有条件通过**(全自动化层验收已通过;真实联调待用户)
- 阻塞问题数:P0 0 项,P1 0 项,P2 0 项,P3 3 项(已识别延后处理)

## 2. 阶段范围与强制边界

- [x] 本阶段功能与 `阶段规划.md` 完全一致(3 表 + admin_user 4 列 / 6 模块 / 6 subscriber / 2 jobs / 6 events / 22 接口 / 7 admin-web 页面)。
- [x] 商家端仅 Android APP / iOS APP,无商家 Web、无商家小程序(stage 4 仅扩 admin-web)。
- [x] 用户端、商家端、骑手端、平台管理端权限隔离(admin-auth.cross-scope.spec.ts:5 测试覆盖 AdminJwtGuard ↔ Customer/Merchant/Rider Guard)。
- [x] 外卖订单与跑腿订单独立(本阶段不涉及订单逻辑;platform_category bizType 严格分 takeaway / errand)。
- [x] 状态、金额、时效、权限、日志均有验收记录(见 ACCEPTANCE_阶段4.md)。

## 3. 文档一致性审查

- [x] 逐条对照需求文档确认本阶段范围(7 份规划文档逐项对照,无遗漏越界)。
- [x] 商家端只 Android/iOS APP(stage 4 仅扩展 admin-web,merchant-app 不动)。
- [x] 每个前端页面都能反查到接口(api/admin-* 7 文件 + DESIGN § 3 接口契约 22 接口)。
- [x] 每个后端接口都能反查到调用端、页面、权限点、审计日志和幂等规则(DESIGN § 3 + § 5 权限矩阵)。
- [x] 状态枚举以后端为唯一权威(admin_user.status / city_site.service_enabled / platform_category.enabled / third_party_config.status 全部 ENUM 或 boolean)。
- [x] 金额本阶段不涉及;时间用毫秒(BIGINT 时间戳全表)。
- [x] 关键写操作有 @Idempotent + @Audit + 服务端事务(admin-role-permission/PUT/admin-user/disable/admin-third-party/PATCH 全覆盖)。
- [x] 第三方走 IntegrationGatewayService(stage 4 第三方配置变更后发 ThirdPartyConfigChanged 事件,subscriber 触发 reloadConfig 钩子;真接入 stage 8+)。

## 4. 接口审查 Checklist

| 接口                                              | 调用端   | Token       | 页面                | 审查结果 | 证据                                                          |
| ------------------------------------------------- | -------- | ----------- | ------------------- | -------- | ------------------------------------------------------------- |
| GET /api/v1/admin/auth/captcha                    | 平台 Web | (无)       | 登录                | 通过     | admin-auth.service.spec.ts createCaptcha 6 用例               |
| POST /api/v1/admin/auth/login                     | 平台 Web | (无)       | 登录                | 通过     | admin-auth.service.spec.ts login 8 用例(锁定 / disabled)    |
| POST /api/v1/admin/auth/refresh                   | 平台 Web | (无)       | 401 自动刷新        | 通过     | admin-auth.service.spec.ts refresh 4 用例                     |
| POST /api/v1/admin/auth/logout                    | 平台 Web | Admin-Token | 退出                | 通过     | admin-auth.service.spec.ts logout 4 用例                      |
| GET /api/v1/admin/riders/applications             | 平台 Web | Admin-Token | 骑手审核            | 通过     | admin-rider.service.spec.ts(stage 3 已通过 + alias 无新逻辑) |
| GET /api/v1/admin/customers/disable-records       | 平台 Web | Admin-Token | 用户禁用记录        | 通过     | admin-user.service.spec.ts listDisableRecords + 路由顺序 P0  |
| POST /api/v1/admin/customers/:id/disable          | 平台 Web | Admin-Token | 用户管理            | 通过     | admin-user.service.spec.ts disable + 事件发布                 |
| POST /api/v1/admin/customers/:id/enable           | 平台 Web | Admin-Token | 用户管理            | 通过     | admin-user.service.spec.ts enable                             |
| GET /api/v1/admin/cities                          | 平台 Web | Admin-Token | 城市站点            | 通过     | admin-city.service.spec.ts list                               |
| POST /api/v1/admin/cities                         | 平台 Web | Admin-Token | 城市站点新增        | 通过     | admin-city.service.spec.ts create + GeoJSON 校验              |
| PATCH /api/v1/admin/cities/:code                  | 平台 Web | Admin-Token | 城市站点编辑        | 通过     | admin-city.service.spec.ts update                             |
| DELETE /api/v1/admin/cities/:code                 | 平台 Web | Admin-Token | 城市站点禁用        | 通过     | admin-city.service.spec.ts disable(软禁用)                  |
| GET /api/v1/admin/categories?bizType              | 平台 Web | Admin-Token | 类目                | 通过     | admin-category.service.spec.ts tree                           |
| POST /api/v1/admin/categories                     | 平台 Web | Admin-Token | 类目新增            | 通过     | admin-category.service.spec.ts create + 重名校验             |
| PATCH /api/v1/admin/categories/:id                | 平台 Web | Admin-Token | 类目编辑            | 通过     | admin-category.service.spec.ts update                         |
| DELETE /api/v1/admin/categories/:id               | 平台 Web | Admin-Token | 类目禁用            | 通过     | admin-category.service.spec.ts disable + HAS_ENABLED_CHILDREN |
| GET /api/v1/admin/system-config                   | 平台 Web | Admin-Token | 系统参数            | 通过     | admin-system-config.service.spec.ts list                      |
| PATCH /api/v1/admin/system-config/:key            | 平台 Web | Admin-Token | 系统参数            | 通过     | admin-system-config.service.spec.ts update + ConfigChanged    |
| GET /api/v1/admin/integrations                    | 平台 Web | Admin-Token | 第三方配置          | 通过     | admin-third-party-config.service.spec.ts list 脱敏            |
| GET /api/v1/admin/integrations/:provider          | 平台 Web | Admin-Token | 第三方配置详情      | 通过     | admin-third-party-config.service.spec.ts detail               |
| PATCH /api/v1/admin/integrations/:provider        | 平台 Web | Admin-Token | 第三方配置编辑      | 通过     | admin-third-party-config.service.spec.ts update + 加密 + 事件 |
| GET /api/v1/admin/roles                           | 平台 Web | Admin-Token | 角色权限            | 通过     | admin-role-permission.service.spec.ts listRoles               |
| GET /api/v1/admin/permissions                     | 平台 Web | Admin-Token | 角色权限            | 通过     | admin-role-permission.service.spec.ts listPermissionTree      |
| PUT /api/v1/admin/roles/:id/permissions           | 平台 Web | Admin-Token | 角色权限保存        | 通过     | admin-role-permission.service.spec.ts update + RoleChanged    |

逐接口手动检查:

- [x] 路径与 DESIGN § 3 接口契约一致(22 接口全核)。
- [x] HTTP Method 正确(GET 列表 / POST 创建 / PATCH 改 / DELETE 软禁用 / PUT 全量替换)。
- [x] 必填字段有 class-validator 校验(全 DTO 均带 IsString/Length/Matches)。
- [x] 状态等关键值后端计算(serviceEnabled / enabled / status 全部 ENUM)。
- [x] 前端无未存在字段(api/admin-*.ts 与后端 DTO 一致)。
- [x] 后端无未处理关键字段(secretMasked 替代 encryptedSecret 输出)。
- [x] 分页、筛选、排序参数一致(pageNo/pageSize 全局统一,城市/用户列表覆盖)。
- [x] 空数据 / 异常数据 / 越权数据返回规范(STATUS_INVALID / FORBIDDEN / NOT_FOUND)。
- [x] 回调接口本阶段不涉及。
- [x] 管理端接口全部 @Audit(login / disable / city/category 改 / role 改 / 第三方 改)。

## 5. 四端联动测试

| 场景                          | 用户端     | 商家端 APP | 骑手端 APP | 平台 Web                       | 结果       | 证据                                              |
| ----------------------------- | ---------- | ---------- | ---------- | ------------------------------ | ---------- | ------------------------------------------------- |
| Admin 登录主流程              | 不涉及     | 不涉及     | 不涉及     | 必测(captcha + login)         | 通过(spec) | admin-auth.service.spec + login/index.spec.ts     |
| 强制下线(用户被禁用)         | 只消费     | 不涉及     | 不涉及     | 必测(disable + AccountDisabled) | 通过(spec) | admin-user-disable + AccountDisabledSubscriber    |
| 角色权限变更广播              | 不涉及     | 不涉及     | 不涉及     | 必测(PUT + RoleChanged)       | 通过(spec) | admin-role-permission + RoleChangedSubscriber     |
| 端隔离(Admin-Token 调 c/m/r)| 必测(返 403) | 必测(返 403) | 必测(返 403) | 必测(自调通过)             | 通过       | admin-auth.cross-scope.spec.ts 5 用例             |
| 第三方配置热更新              | 不涉及     | 不涉及     | 不涉及     | 必测(secret 加密 + 事件)     | 通过(spec) | admin-third-party-config + ThirdPartyConfigChanged |

## 5.1 本阶段端侧适用性说明

| 端                         | 本阶段状态 | 手动审查要求                                                                          |
| -------------------------- | ---------- | ------------------------------------------------------------------------------------- |
| 用户端                     | 只消费     | 不新增页面;仅在 disable 端被广播失效。需用户抽测被禁用账号的 token 失效              |
| 商家端 Android/iOS APP     | 只消费     | 同上                                                                                  |
| 骑手端 Android/iOS APP     | 只消费     | 同上                                                                                  |
| 平台管理 Web               | 必测       | 7 类页面(login/customers/cities/categories/system-config/integrations/roles)全交付 |

## 6. 第三方联调

- [x] 地图:本阶段不涉及(GeoJSON 仅 textarea 录入,真渲染待 stage 8)。
- [x] 支付:本阶段不涉及。
- [x] 短信:本阶段不涉及。
- [x] 推送:本阶段不涉及。
- [x] 实名:本阶段不涉及(stage 1 已就位)。
- [x] 文件/对象存储:本阶段不涉及。
- [x] 第三方失败场景:本阶段第三方配置变更后 reloadConfig 失败 warn 不阻塞主流程(audit_log 记录)。
- [x] 禁止真实分支仍为 Mock:本阶段无真实第三方接入(SUPER_ADMIN seed 默认密码已加密 bcrypt cost=10)。

## 6.1 第三方适用性记录

| 第三方       | 本阶段结论 | 不适用原因/延后批准                  | 凭证 | 失败日志 |
| ------------ | ---------- | ------------------------------------ | ---- | -------- |
| 高德地图     | 不适用     | stage 8 城市/配送区域 polygon editor | —    | —        |
| 支付         | 不适用     | stage 5 订单                         | —    | —        |
| 阿里云短信   | 不适用     | stage 11 真接入                      | —    | —        |
| 个推         | 不适用     | stage 11 真接入                      | —    | —        |
| 阿里云实名   | 不适用     | stage 11 真接入                      | —    | —        |
| MinIO 文件   | 不适用     | 本阶段无文件上传                     | —    | —        |

## 7. 问题清单

| 编号       | 级别 | 发现时间   | 文件/接口                   | 问题                                       | 影响                          | 修复标准                              | 状态     |
| ---------- | ---- | ---------- | --------------------------- | ------------------------------------------ | ----------------------------- | ------------------------------------- | -------- |
| 阶段 4 P3-01 | P3 | 2026-05-05 | admin-web cities GeoJSON | textarea 录入 GeoJSON 体验差               | 真拖拽体验差                  | stage 8 接高德 polygon editor         | 已登记延后 |
| 阶段 4 P3-02 | P3 | 2026-05-05 | RoleChanged jti 黑名单     | 仅写 admin:role-revoked 标记,未真撤销 jti | 老 token 仍可用直至自然过期   | stage 5+ 接 access token iat 比对     | 已登记延后 |
| 阶段 4 P3-03 | P3 | 2026-05-05 | third-party reloadConfig    | 当前仅事件发布,真热加载在 stage 8+        | 改 secret 后需重启服务才生效  | stage 8 IntegrationGateway 真热更新   | 已登记延后 |

## 7.1 修复阶段历史问题清单

| 编号       | 级别 | 修复时间   | 文件                                                                  | 问题                                                                               | 修复方案                                          |
| ---------- | ---- | ---------- | --------------------------------------------------------------------- | ---------------------------------------------------------------------------------- | ------------------------------------------------- |
| 阶段 4 F-01 | P0   | 2026-05-05 | apps/server/src/modules/admin-user/admin-user.controller.ts           | @Get(':id') 在 @Get('disable-records') 之前,disable-records 请求被 :id 吞         | 调换顺序:具体路径段先注册,动态参数后注册        |
| 阶段 4 F-02 | P1   | 2026-05-05 | apps/admin-web/src/api/admin-auth.ts                                  | 缺 refresh() 调用函数,导致 401 无法自动刷新                                       | 补 RefreshReq/Vo 类型 + refresh() 函数            |
| 阶段 4 F-03 | P1   | 2026-05-05 | apps/admin-web/src/utils/request.ts                                   | 401 直接 clearAuth,未尝试 refresh                                                 | 实现单飞 + 队列 401 自动刷新机制                  |
| 阶段 4 F-04 | P1   | 2026-05-05 | apps/admin-web/src/views/login/index.vue, layout/index.vue           | UI 不显示 lastLoginAt                                                              | 登录成功 toast + layout header 显示               |
| 阶段 4 F-05 | P1   | 2026-05-05 | apps/admin-web/src/views/login/index.vue                              | 无 ACCOUNT_LOCKED modal 提示                                                       | STATUS_INVALID 错误码 → ElMessageBox.alert       |

## 8. 是否允许进入下一阶段

- [x] P0 已清零(F-01 已修复)。
- [x] P1 已清零(F-02 / F-03 / F-04 / F-05 已修复)。
- [x] 接口审查无阻塞问题(22 接口全部覆盖 spec)。
- [x] 四端边界无违规(用户/商家/骑手只消费,平台 Web 必测)。
- [x] 后端 server tests 53 suites / 371 tests 全绿。
- [x] 前端 admin-web tests 20 files / 57 tests 全绿。
- [x] 审查人签字:Claude(2026-05-05)
- [ ] 复核人签字:**等用户人工抽测后签字**(MySQL migration + curl + admin-web 浏览器抽测)
