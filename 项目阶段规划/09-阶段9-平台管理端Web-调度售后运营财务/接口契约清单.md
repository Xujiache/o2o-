# 阶段9-平台管理端Web-调度售后运营财务 接口契约清单

> 商家端仅 Android APP / iOS APP，禁止规划商家 Web 后台、商家小程序。外卖订单与跑腿订单必须独立订单体系、独立状态机、独立计价、独立退款规则。

## 全局接口规范

统一前缀：用户端 `/api/v1/c/**`，商家端 `/api/v1/m/**`，骑手端 `/api/v1/r/**`，平台 Web `/api/v1/admin/**`，公开 `/api/v1/pub/**`，第三方回调 `/api/v1/callback/**`。各端仅使用对应 Token：用户端只用 `Customer-Token`，商家 Android/iOS APP 只用 `Merchant-Token`，骑手 Android/iOS APP 只用 `Rider-Token`，平台 Web 只用 `Admin-Token`。

## 本阶段接口详情

### GET /api/v1/admin/food-orders

- 接口名称：全量外卖订单
- 调用端：平台 Web
- 鉴权 Token：Admin-Token
- 权限点：`admin:food:orders:view`，平台 Web 必须校验管理员菜单权限、按钮权限和数据范围。
- 使用页面/场景：外卖订单列表
- 请求字段：orderNo、userKeyword、merchantKeyword、status、timeRange、pageNo、pageSize
- 响应字段：分页订单、金额、状态、商家、骑手
- 状态/枚举：以后端阶段状态机枚举为准，前端不得硬编码中文状态。
- 错误码：`INVALID_PARAM`、`UNAUTHORIZED`、`FORBIDDEN`、`DATA_NOT_FOUND`、`STATUS_INVALID`、`DUPLICATE_REQUEST`、`THIRD_PARTY_ERROR`
- 幂等要求：写操作必须支持 `Idempotency-Key`；重复请求返回同一业务结果，不重复扣款、不重复写业务变更。
- 审计日志：记录 traceId、operatorType、operatorId、targetType、targetId、beforeStatus、afterStatus、ip、deviceId、createdAt。
- 前端对接注意：页面必须处理 loading、空状态、错误提示、防重复点击和 token 过期跳转。

### GET /api/v1/admin/errand-orders

- 接口名称：全量跑腿订单
- 调用端：平台 Web
- 鉴权 Token：Admin-Token
- 权限点：`admin:errand:orders:view`，平台 Web 必须校验管理员菜单权限、按钮权限和数据范围。
- 使用页面/场景：跑腿订单列表
- 请求字段：orderNo、type、status、timeRange、pageNo、pageSize
- 响应字段：分页跑腿订单、报价、状态、骑手
- 状态/枚举：以后端阶段状态机枚举为准，前端不得硬编码中文状态。
- 错误码：`INVALID_PARAM`、`UNAUTHORIZED`、`FORBIDDEN`、`DATA_NOT_FOUND`、`STATUS_INVALID`、`DUPLICATE_REQUEST`、`THIRD_PARTY_ERROR`
- 幂等要求：写操作必须支持 `Idempotency-Key`；重复请求返回同一业务结果，不重复扣款、不重复写业务变更。
- 审计日志：记录 traceId、operatorType、operatorId、targetType、targetId、beforeStatus、afterStatus、ip、deviceId、createdAt。
- 前端对接注意：页面必须处理 loading、空状态、错误提示、防重复点击和 token 过期跳转。

### POST /api/v1/admin/dispatch/tasks/{taskId}/assign

- 接口名称：人工派单
- 调用端：平台 Web
- 鉴权 Token：Admin-Token
- 权限点：`admin:dispatch:manage`，平台 Web 必须校验管理员菜单权限、按钮权限和数据范围。
- 使用页面/场景：调度监控
- 请求字段：taskId、riderId、reason
- 响应字段：taskId、dispatchStatus、assignedAt
- 状态/枚举：以后端阶段状态机枚举为准，前端不得硬编码中文状态。
- 错误码：`INVALID_PARAM`、`UNAUTHORIZED`、`FORBIDDEN`、`DATA_NOT_FOUND`、`STATUS_INVALID`、`DUPLICATE_REQUEST`、`THIRD_PARTY_ERROR`
- 幂等要求：写操作必须支持 `Idempotency-Key`；重复请求返回同一业务结果，不重复扣款、不重复写业务变更。
- 审计日志：记录 traceId、operatorType、operatorId、targetType、targetId、beforeStatus、afterStatus、ip、deviceId、createdAt。
- 前端对接注意：页面必须处理 loading、空状态、错误提示、防重复点击和 token 过期跳转。

### POST /api/v1/admin/after-sales/{afterSaleId}/arbitrate

- 接口名称：售后仲裁
- 调用端：平台 Web
- 鉴权 Token：Admin-Token
- 权限点：`admin:after:sales:manage`，平台 Web 必须校验管理员菜单权限、按钮权限和数据范围。
- 使用页面/场景：售后仲裁详情
- 请求字段：responsibleParty、decision、refundAmount、penalty、remark
- 响应字段：afterSaleId、status、refundOrderId
- 状态/枚举：以后端阶段状态机枚举为准，前端不得硬编码中文状态。
- 错误码：`INVALID_PARAM`、`UNAUTHORIZED`、`FORBIDDEN`、`DATA_NOT_FOUND`、`STATUS_INVALID`、`DUPLICATE_REQUEST`、`THIRD_PARTY_ERROR`
- 幂等要求：写操作必须支持 `Idempotency-Key`；重复请求返回同一业务结果，不重复扣款、不重复写业务变更。
- 审计日志：记录 traceId、operatorType、operatorId、targetType、targetId、beforeStatus、afterStatus、ip、deviceId、createdAt。
- 前端对接注意：页面必须处理 loading、空状态、错误提示、防重复点击和 token 过期跳转。

### POST /api/v1/admin/marketing/coupons

- 接口名称：发布优惠券
- 调用端：平台 Web
- 鉴权 Token：Admin-Token
- 权限点：`admin:marketing:manage`，平台 Web 必须校验管理员菜单权限、按钮权限和数据范围。
- 使用页面/场景：优惠券配置
- 请求字段：couponType、bizType、threshold、discount、stock、validTime
- 响应字段：couponId、status
- 状态/枚举：以后端阶段状态机枚举为准，前端不得硬编码中文状态。
- 错误码：`INVALID_PARAM`、`UNAUTHORIZED`、`FORBIDDEN`、`DATA_NOT_FOUND`、`STATUS_INVALID`、`DUPLICATE_REQUEST`、`THIRD_PARTY_ERROR`
- 幂等要求：写操作必须支持 `Idempotency-Key`；重复请求返回同一业务结果，不重复扣款、不重复写业务变更。
- 审计日志：记录 traceId、operatorType、operatorId、targetType、targetId、beforeStatus、afterStatus、ip、deviceId、createdAt。
- 前端对接注意：页面必须处理 loading、空状态、错误提示、防重复点击和 token 过期跳转。

### PATCH /api/v1/admin/rate-rules

- 接口名称：费率配置
- 调用端：平台 Web
- 鉴权 Token：Admin-Token
- 权限点：`admin:rate:rules:manage`，平台 Web 必须校验管理员菜单权限、按钮权限和数据范围。
- 使用页面/场景：费率配置
- 请求字段：cityCode、categoryId、merchantCommissionRate、riderServiceFee、withdrawFeeRate、settlementCycle
- 响应字段：ruleId、effectiveAt
- 状态/枚举：以后端阶段状态机枚举为准，前端不得硬编码中文状态。
- 错误码：`INVALID_PARAM`、`UNAUTHORIZED`、`FORBIDDEN`、`DATA_NOT_FOUND`、`STATUS_INVALID`、`DUPLICATE_REQUEST`、`THIRD_PARTY_ERROR`
- 幂等要求：写操作必须支持 `Idempotency-Key`；重复请求返回同一业务结果，不重复扣款、不重复写业务变更。
- 审计日志：记录 traceId、operatorType、operatorId、targetType、targetId、beforeStatus、afterStatus、ip、deviceId、createdAt。
- 前端对接注意：页面必须处理 loading、空状态、错误提示、防重复点击和 token 过期跳转。

### GET /api/v1/admin/dashboard/overview

- 接口名称：数据大屏
- 调用端：平台 Web
- 鉴权 Token：Admin-Token
- 权限点：`admin:dashboard:view`，平台 Web 必须校验管理员菜单权限、按钮权限和数据范围。
- 使用页面/场景：数据大屏
- 请求字段：cityCode、timeRange
- 响应字段：gmv、orderCount、activeUsers、onlineRiders、exceptionOrders
- 状态/枚举：以后端阶段状态机枚举为准，前端不得硬编码中文状态。
- 错误码：`INVALID_PARAM`、`UNAUTHORIZED`、`FORBIDDEN`、`DATA_NOT_FOUND`、`STATUS_INVALID`、`DUPLICATE_REQUEST`、`THIRD_PARTY_ERROR`
- 幂等要求：写操作必须支持 `Idempotency-Key`；重复请求返回同一业务结果，不重复扣款、不重复写业务变更。
- 审计日志：记录 traceId、operatorType、operatorId、targetType、targetId、beforeStatus、afterStatus、ip、deviceId、createdAt。
- 前端对接注意：页面必须处理 loading、空状态、错误提示、防重复点击和 token 过期跳转。


## 接口审查清单

- [ ] 每个接口有调用端、Token、权限点。
- [ ] 每个接口有请求字段、响应字段、字段单位。
- [ ] 每个接口有错误码和前端展示策略。
- [ ] 每个写接口有幂等规则。
- [ ] 每个关键写接口有审计日志。
- [ ] 每个状态变更接口写明 beforeStatus 和 afterStatus。
- [ ] 每个接口能反查前端页面。
- [ ] 每个前端页面能反查接口。
- [ ] 第三方回调有验签、去重、防重放。
