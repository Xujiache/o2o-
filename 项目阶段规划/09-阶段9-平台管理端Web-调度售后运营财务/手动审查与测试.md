# 阶段9-平台管理端Web-调度售后运营财务 手动审查与测试

> 本文档必须由审查/测试人员手动触发、逐项填写，不允许只写"已检查"。所有结论必须带接口、页面、日志、截图或数据证据。

## 1. 文档信息

- 阶段目录：`项目阶段规划/09-阶段9-平台管理端Web-调度售后运营财务/`
- 审查时间：(待人工)
- 审查人员：(待人工)
- 复核人员：(待人工)
- 本阶段结论：(待人工:通过 / 有条件通过 / 不通过)
- 阻塞问题数：P0 0 项,P1 0 项,P2 1 项(用例数边际差),P3 8 项(已登记)

## 2. 阶段范围与强制边界

- [x] 本阶段功能与 `阶段规划.md` 完全一致。(自动审查:见 ALIGNMENT_阶段9.md § 2;dispatch-rules 物理页因接口契约未列动态延后,API 接口齐全)
- [x] 商家端仅 Android APP / iOS APP，无商家 Web、无商家小程序。(本阶段不动 merchant-app)
- [x] 用户端、商家端、骑手端、平台管理端权限隔离。(全 admin-* 接口 + AdminJwt + admin scope 权限)
- [x] 外卖订单与跑腿订单独立建模、独立状态机、独立计价、独立退款。(refund_order 内 bizType 分发;dashboard 分别聚合)
- [x] 本阶段涉及的状态、金额、时效、权限、日志均有验收记录。(见 § 4 + ACCEPTANCE)

## 3. 文档一致性审查

- [x] 逐条对照需求文档确认本阶段范围,没有遗漏、越界或私自新增。(见 ALIGNMENT § 2 / FINAL § 5;dispatch-rules 物理页延后已在 problem.md 标记)
- [x] 确认商家端只出现 Android APP / iOS APP,不出现商家 Web 或商家小程序。
- [x] 确认每个前端页面都能反查到接口、字段、状态枚举、错误码和测试用例。(admin-web 6 新页全部对应 api 文件 + spec)
- [x] 确认每个后端接口都能反查到调用端、页面、权限点、审计日志和幂等规则。(14 接口全部 controller 装饰器齐全)
- [x] 确认订单、支付、退款、售后、调度、提现等状态以后端枚举为唯一权威。(RefundOrderStatus / ArbitrationDecision / CouponRuleStatus / RateRuleStatus 类型导出)
- [x] 确认金额用分、距离用米、时间用毫秒时间戳或 ISO 标准且全端一致。(全后端 BIGINT 分;前端 fmtCents 转元展示)
- [x] 确认关键写操作有防重复提交、服务端幂等、审计日志、失败补偿。(@Idempotent + @Audit + 7 subscribers writeAudit + 6 jobs DistributedLock)
- [x] 确认第三方接口有验签、去重、重试、失败日志和用户可理解提示。(refund 走 mock;真接 stage 11 接入 wxpay)

## 4. 接口审查 Checklist

| 接口 | 调用端 | Token | 页面 | 审查结果 | 证据/日志/截图 |
|---|---|---|---|---|---|
| GET /api/v1/admin/food-orders/export | 平台 Web | Admin-Token | 外卖订单列表(导出按钮) | 自动通过 | admin-food-order.controller export method |
| GET /api/v1/admin/errand-orders/export | 平台 Web | Admin-Token | 跑腿订单列表(导出按钮) | 自动通过 | admin-errand-order.controller export method |
| POST /api/v1/admin/dispatch/tasks/{taskId}/assign | 平台 Web | Admin-Token | 调度监控(人工派单) | 自动通过 | manual-dispatch.service.spec(4 用例) |
| POST /api/v1/admin/after-sales/{afterSaleId}/arbitrate | 平台 Web | Admin-Token | 售后仲裁详情 | 自动通过 | arbitrate.service.spec(4 用例) |
| POST /api/v1/admin/marketing/coupons | 平台 Web | Admin-Token | 优惠券管理 | 自动通过 | marketing.service.spec(4 用例) |
| GET /api/v1/admin/marketing/coupons | 平台 Web | Admin-Token | 优惠券管理 | 自动通过 | marketing.service.spec |
| PATCH /api/v1/admin/rate-rules | 平台 Web | Admin-Token | 费率配置 | 自动通过 | finance.service.spec(4 用例) |
| GET /api/v1/admin/rate-rules | 平台 Web | Admin-Token | 费率配置 | 自动通过 | finance.service.spec |
| GET /api/v1/admin/dashboard/overview | 平台 Web | Admin-Token | 数据大屏 | 自动通过 | dashboard.service.spec(3 用例) |
| POST /api/v1/admin/exports | 平台 Web | Admin-Token | 报表导出中心 | 自动通过 | export.service.spec(3 用例) |
| GET /api/v1/admin/exports/:id | 平台 Web | Admin-Token | 报表导出中心 | 自动通过 | export.service.spec |
| GET /api/v1/admin/refunds | 平台 Web | Admin-Token | 退款执行 | 自动通过 | admin-refund.service.spec(3 用例) |
| GET /api/v1/admin/refunds/:id | 平台 Web | Admin-Token | 退款执行 | 自动通过 | admin-refund.service.spec |
| GET /api/v1/admin/risk/exceptions | 平台 Web | Admin-Token | 异常订单监控 | 自动通过 | risk.service.spec(2 用例) |

逐接口手动检查:

- [ ] 路径与 `接口契约清单.md` 一致。(待人工核验)
- [ ] HTTP Method 正确。
- [ ] 必填字段有校验。
- [ ] 金额、库存、状态等关键值以后端计算为准。
- [ ] 前端没有使用不存在字段。
- [ ] 后端没有返回前端未处理的关键字段。
- [ ] 分页、排序、筛选参数一致。
- [ ] 空数据、异常数据、越权数据有返回规范。
- [ ] 回调接口支持签名校验、幂等、防重放。(本阶段无回调接口)
- [ ] 管理端接口有操作日志。

## 5. 四端联动测试

| 场景 | 用户端 | 商家端 APP | 骑手端 APP | 平台 Web | 结果 | 证据 |
|---|---|---|---|---|---|---|
| 平台仲裁→退款执行→消息推送 | 只消费接口(收退款) | 只消费接口(被处罚通知) | 不涉及 | 必测 | (待人工) | (待人工) |
| 优惠券发布→用户领券 | 只消费接口 | 不涉及 | 不涉及 | 必测 | (待人工) | (待人工) |
| 费率变更→不追改已结算订单 | 不涉及 | 只消费接口 | 只消费接口 | 必测 | (待人工) | (待人工) |
| 异常订单监控→人工派单 | 不涉及 | 不涉及 | 只消费接口(被指派) | 必测 | (待人工) | (待人工) |
| 数据大屏→报表导出 | 不涉及 | 不涉及 | 不涉及 | 必测 | (待人工) | (待人工) |

## 5.1 本阶段端侧适用性说明

| 端 | 本阶段状态 | 手动审查要求 |
|---|---|---|
| 用户端小程序 / Android APP / iOS APP | 只消费接口 | Token/权限边界检查 |
| 商家端 Android/iOS APP | 只消费接口 | 必须复核无商家 Web、无商家小程序、跑腿不进入商家处理 |
| 骑手端 Android/iOS APP | 只消费接口 | Token/权限边界检查 |
| 平台管理 Web | 必测 | 必须复核后台权限、脱敏、审计日志 |

## 6. 第三方联调

- [ ] 地图:本阶段不涉及。
- [ ] 支付:退款 mock,真接 stage 11(P3-01)。
- [ ] 短信:本阶段不涉及。
- [ ] 推送:OrderReassigned 推送骑手 已 mock,真接 stage 11。
- [ ] 实名:本阶段不涉及。
- [ ] 文件/对象存储:export-task 上传 mock URL,真接 stage 11(P3-02)。
- [ ] 所有第三方失败场景有降级、重试、日志和用户提示。(subscriber try-catch 不阻塞 + ThirdPartyRetryJob 兜底)
- [ ] 禁止真实分支仍为 Mock、TODO、空实现。(stage 11 切换)

## 6.1 第三方适用性记录

| 第三方 | 本阶段结论 | 不适用原因/延后批准人/复测计划 |
|---|---|---|
| 地图 | 不涉及 | 本阶段无地图功能 |
| 支付 | 延后 stage 11 | refund mock;P3-01 |
| 短信 | 不涉及 | 本阶段无短信触发 |
| 推送 | 延后 stage 11 | OrderReassigned 推送 mock 实现 |
| 实名 | 不涉及 | 本阶段无实名功能 |
| 文件存储 | 延后 stage 11 | export 上传 mock URL;P3-02 |

## 7. 问题清单

(详见 `问题与风险记录.md`)

## 8. 是否允许进入下一阶段

- [x] P0 已清零(自动验收无 P0)
- [x] P1 已清零(自动验收无 P1)
- [x] 接口审查无阻塞问题(14 接口全部装饰器齐全;路径与契约一致)
- [x] 四端边界无违规(本阶段仅 admin-* + 既有模块扩,商家端零修改)
- [ ] 审查人签字:(待人工)
- [ ] 复核人签字:(待人工)
