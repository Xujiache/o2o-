# 阶段8-骑手端APP-调度轨迹收益考核 接口契约清单

> 商家端仅 Android APP / iOS APP，禁止规划商家 Web 后台、商家小程序。外卖订单与跑腿订单必须独立订单体系、独立状态机、独立计价、独立退款规则。

## 全局接口规范

统一前缀：用户端 `/api/v1/c/**`，商家端 `/api/v1/m/**`，骑手端 `/api/v1/r/**`，平台 Web `/api/v1/admin/**`，公开 `/api/v1/pub/**`，第三方回调 `/api/v1/callback/**`。各端仅使用对应 Token：用户端只用 `Customer-Token`，商家 Android/iOS APP 只用 `Merchant-Token`，骑手 Android/iOS APP 只用 `Rider-Token`，平台 Web 只用 `Admin-Token`。

## 本阶段接口详情

### POST /api/v1/r/tasks/{taskId}/accept

- 接口名称：抢单/接单
- 调用端：骑手 Android/iOS APP
- 鉴权 Token：Rider-Token
- 权限点：`rider:self` + `rider:task:own-or-available`，骑手只能访问本人资料、可接任务或已分配任务。
- 使用页面/场景：派单弹窗/接单大厅
- 请求字段：taskId、lng、lat、idempotencyKey
- 响应字段：taskId、orderId、bizType、taskStatus
- 状态/枚举：以后端阶段状态机枚举为准，前端不得硬编码中文状态。
- 错误码：`INVALID_PARAM`、`UNAUTHORIZED`、`FORBIDDEN`、`DATA_NOT_FOUND`、`STATUS_INVALID`、`DUPLICATE_REQUEST`、`THIRD_PARTY_ERROR`
- 幂等要求：写操作必须支持 `Idempotency-Key`；重复请求返回同一业务结果，不重复扣款、不重复写业务变更。
- 审计日志：记录 traceId、operatorType、operatorId、targetType、targetId、beforeStatus、afterStatus、ip、deviceId、createdAt。
- 前端对接注意：页面必须处理 loading、空状态、错误提示、防重复点击和 token 过期跳转。

### POST /api/v1/r/tasks/{taskId}/arrive-pickup

- 接口名称：到达取货点
- 调用端：骑手 Android/iOS APP
- 鉴权 Token：Rider-Token
- 权限点：`rider:self` + `rider:task:own-or-available`，骑手只能访问本人资料、可接任务或已分配任务。
- 使用页面/场景：导航到店/取货点
- 请求字段：taskId、lng、lat
- 响应字段：taskId、status、arrivedAt
- 状态/枚举：以后端阶段状态机枚举为准，前端不得硬编码中文状态。
- 错误码：`INVALID_PARAM`、`UNAUTHORIZED`、`FORBIDDEN`、`DATA_NOT_FOUND`、`STATUS_INVALID`、`DUPLICATE_REQUEST`、`THIRD_PARTY_ERROR`
- 幂等要求：写操作必须支持 `Idempotency-Key`；重复请求返回同一业务结果，不重复扣款、不重复写业务变更。
- 审计日志：记录 traceId、operatorType、operatorId、targetType、targetId、beforeStatus、afterStatus、ip、deviceId、createdAt。
- 前端对接注意：页面必须处理 loading、空状态、错误提示、防重复点击和 token 过期跳转。

### POST /api/v1/r/tasks/{taskId}/pickup

- 接口名称：确认取货
- 调用端：骑手 Android/iOS APP
- 鉴权 Token：Rider-Token
- 权限点：`rider:self` + `rider:task:own-or-available`，骑手只能访问本人资料、可接任务或已分配任务。
- 使用页面/场景：取货确认页
- 请求字段：taskId、pickupCode、itemCheckResult、photos
- 响应字段：taskId、status、pickedUpAt
- 状态/枚举：以后端阶段状态机枚举为准，前端不得硬编码中文状态。
- 错误码：`INVALID_PARAM`、`UNAUTHORIZED`、`FORBIDDEN`、`DATA_NOT_FOUND`、`STATUS_INVALID`、`DUPLICATE_REQUEST`、`THIRD_PARTY_ERROR`
- 幂等要求：写操作必须支持 `Idempotency-Key`；重复请求返回同一业务结果，不重复扣款、不重复写业务变更。
- 审计日志：记录 traceId、operatorType、operatorId、targetType、targetId、beforeStatus、afterStatus、ip、deviceId、createdAt。
- 前端对接注意：页面必须处理 loading、空状态、错误提示、防重复点击和 token 过期跳转。

### POST /api/v1/r/tasks/{taskId}/delivered

- 接口名称：确认送达
- 调用端：骑手 Android/iOS APP
- 鉴权 Token：Rider-Token
- 权限点：`rider:self` + `rider:task:own-or-available`，骑手只能访问本人资料、可接任务或已分配任务。
- 使用页面/场景：送达确认页
- 请求字段：taskId、deliveryProof、lng、lat
- 响应字段：taskId、status、deliveredAt
- 状态/枚举：以后端阶段状态机枚举为准，前端不得硬编码中文状态。
- 错误码：`INVALID_PARAM`、`UNAUTHORIZED`、`FORBIDDEN`、`DATA_NOT_FOUND`、`STATUS_INVALID`、`DUPLICATE_REQUEST`、`THIRD_PARTY_ERROR`
- 幂等要求：写操作必须支持 `Idempotency-Key`；重复请求返回同一业务结果，不重复扣款、不重复写业务变更。
- 审计日志：记录 traceId、operatorType、operatorId、targetType、targetId、beforeStatus、afterStatus、ip、deviceId、createdAt。
- 前端对接注意：页面必须处理 loading、空状态、错误提示、防重复点击和 token 过期跳转。

### POST /api/v1/r/tasks/{taskId}/exception

- 接口名称：异常报备
- 调用端：骑手 Android/iOS APP
- 鉴权 Token：Rider-Token
- 权限点：`rider:self` + `rider:task:own-or-available`，骑手只能访问本人资料、可接任务或已分配任务。
- 使用页面/场景：异常报备页
- 请求字段：exceptionType、description、photos、lng、lat
- 响应字段：exceptionId、status、platformHandleRequired
- 状态/枚举：以后端阶段状态机枚举为准，前端不得硬编码中文状态。
- 错误码：`INVALID_PARAM`、`UNAUTHORIZED`、`FORBIDDEN`、`DATA_NOT_FOUND`、`STATUS_INVALID`、`DUPLICATE_REQUEST`、`THIRD_PARTY_ERROR`
- 幂等要求：写操作必须支持 `Idempotency-Key`；重复请求返回同一业务结果，不重复扣款、不重复写业务变更。
- 审计日志：记录 traceId、operatorType、operatorId、targetType、targetId、beforeStatus、afterStatus、ip、deviceId、createdAt。
- 前端对接注意：页面必须处理 loading、空状态、错误提示、防重复点击和 token 过期跳转。

### GET /api/v1/r/earnings

- 接口名称：收益中心
- 调用端：骑手 Android/iOS APP
- 鉴权 Token：Rider-Token
- 权限点：`rider:self` + `rider:task:own-or-available`，骑手只能访问本人资料、可接任务或已分配任务。
- 使用页面/场景：收益中心
- 请求字段：dateRange、pageNo、pageSize
- 响应字段：totalIncome、orderCount、rewardAmount、deductAmount、items
- 状态/枚举：以后端阶段状态机枚举为准，前端不得硬编码中文状态。
- 错误码：`INVALID_PARAM`、`UNAUTHORIZED`、`FORBIDDEN`、`DATA_NOT_FOUND`、`STATUS_INVALID`、`DUPLICATE_REQUEST`、`THIRD_PARTY_ERROR`
- 幂等要求：写操作必须支持 `Idempotency-Key`；重复请求返回同一业务结果，不重复扣款、不重复写业务变更。
- 审计日志：记录 traceId、operatorType、operatorId、targetType、targetId、beforeStatus、afterStatus、ip、deviceId、createdAt。
- 前端对接注意：页面必须处理 loading、空状态、错误提示、防重复点击和 token 过期跳转。

### POST /api/v1/r/withdrawals

- 接口名称：骑手提现
- 调用端：骑手 Android/iOS APP
- 鉴权 Token：Rider-Token
- 权限点：`rider:self` + `rider:task:own-or-available`，骑手只能访问本人资料、可接任务或已分配任务。
- 使用页面/场景：提现页
- 请求字段：amount、accountId、smsCode、idempotencyKey
- 响应字段：withdrawalId、status
- 状态/枚举：以后端阶段状态机枚举为准，前端不得硬编码中文状态。
- 错误码：`INVALID_PARAM`、`UNAUTHORIZED`、`FORBIDDEN`、`DATA_NOT_FOUND`、`STATUS_INVALID`、`DUPLICATE_REQUEST`、`THIRD_PARTY_ERROR`
- 幂等要求：写操作必须支持 `Idempotency-Key`；重复请求返回同一业务结果，不重复扣款、不重复写业务变更。
- 审计日志：记录 traceId、operatorType、operatorId、targetType、targetId、beforeStatus、afterStatus、ip、deviceId、createdAt。
- 前端对接注意：页面必须处理 loading、空状态、错误提示、防重复点击和 token 过期跳转。


## 接口审查清单

- [ ] 每个接口有调用端、Token、权限点。
- [ ] 每个接口有请求字段、响应字段、字段单位。
- [ ] 每个接口有错误码和前端展示策略。
- [ ] 每个写接口有幂等规则。
- [ ] 每个关键写接口有审计日志。
- [ ] 每个状态变更接口写明 beforeStatus 和 afterStatus。
- [ ] 每个接口能反查前端页面。
- [ ] 每个前端页面能反查接口。
- [ ] 第三方回调有验签、去重、防重放。
