# 阶段1-用户端-账号地址与基础框架 手动审查与测试

> 本文档由 Claude(实现者兼自审者)在 stage 1 交付时填写。所有结论引用 spec / 文件 / 提交 hash 作为证据;真机/真接口验证标"待人工抽测"。

## 1. 文档信息

- 阶段目录:`项目阶段规划/01-阶段1-用户端-账号地址与基础框架/`
- 审查时间:2026-05-05
- 审查人员:Claude(实现者自审)
- 复核人员:待人工复核
- 本阶段结论:**有条件通过**(需人工抽测真机 + 真 MySQL/Redis 跑通 migration)
- 阻塞问题数:**P0 0 项,P1 0 项,P2 1 项(已修),P3 0 项**

## 2. 阶段范围与强制边界

- [x] 本阶段功能与 `阶段规划.md` 完全一致(8 表 + 12 接口 + 5 事件 + 4 任务 + 11 用户端页 + 4 平台 Web 页)。
- [x] 商家端无 Web 后台、无小程序;本阶段未涉及商家端,商家端 stage 2 起处理。
- [x] 4 端权限隔离:`scope-jwt.guard.ts` + `customer-auth.cross-scope.spec.ts` 验证 customer-Token 调 admin 接口 → FORBIDDEN(7 测试)。
- [x] 跑腿/外卖订单不在本阶段范围(stage 5/6)。
- [x] 状态、权限、日志:全 service 接 @Audit + 5 领域事件落 `domain_event` 表。

## 3. 文档一致性审查

- [x] 本阶段范围逐条对照 `阶段规划.md` / `接口契约清单.md`,无遗漏、无越界。证据:`docs/阶段1-用户账号地址/TASK_阶段1.md` 的 20 个 T 任务全部完成 → `git log --oneline 138d4a0`。
- [x] 商家端无 Web/小程序产物 — `apps/` 下仅 customer-app / merchant-app(APP) / rider-app(APP) / admin-web(平台)。
- [x] 11 个用户端页面全部能反查接口与状态枚举 — 见 `按端实施范围.md` 与 `apps/customer-app/src/pages/` 实际目录。
- [x] 12 个后端接口全部带 `@RequirePermission` / `@Audit` / `@Idempotent` — 见 `apps/server/src/modules/{customer-auth,realname,address,admin-user,sms}/*.controller.ts`。
- [x] 状态枚举以后端 entity 为权威 — 8 个 entity 用 TypeScript 联合类型导出,前端从 `@o2o/contracts` 间接消费。
- [x] 金额本阶段不涉及。距离/坐标:`customer_address.lng/lat` DECIMAL(10,7),`MapView.vue` 输入 latitude/longitude 数值。
- [x] 时间戳:全表 `BIGINT` 毫秒(沿用 stage 0 约定,覆盖 DESIGN 文档字面 DATETIME(3))。
- [x] 关键写操作均有防重复:`@Idempotent({ scope: ..., ttlSeconds: 60 })` + `IdempotencyInterceptor`(stage 0)。
- [x] 第三方接入只能 mock — `INTEGRATION_MODE=mock` 下 sms / realname / wxlogin 全部 mock,real 分支抛 `not configured`。

## 4. 接口审查 Checklist

| 接口 | 调用端 | Token | 权限点 | 幂等 | 审计 | 测试证据 |
|---|---|---|---|---|---|---|
| POST /api/v1/c/auth/sms-code | 用户端 | 无 | customer:public(公开) | 60s | 否 | `sms.service.spec.ts`(6) |
| POST /api/v1/c/auth/login | 用户端 | 无 | — | 60s | 是 | `customer-auth.service.spec.ts`(8) |
| POST /api/v1/c/auth/wechat-login | 用户端 | 无 | — | 60s | 是 | `customer-auth.service.spec.ts` 微信首次 → bindMobileRequired |
| POST /api/v1/c/auth/refresh | 用户端 | 无(refresh) | — | 60s | 否 | `customer-auth.service.spec.ts` refresh 轮换 |
| POST /api/v1/c/auth/logout | 用户端 | Customer-Token | — | 60s | 是 | `customer-auth.service.spec.ts` jti 黑名单 + revoke 设备 |
| POST /api/v1/c/realname/verify | 用户端 | Customer-Token | customer:self | 60s | 是 | `realname.service.spec.ts`(5) |
| GET /api/v1/c/addresses | 用户端 | Customer-Token | customer:self | — | 否 | `address.service.spec.ts` 排序 + 脱敏 |
| POST /api/v1/c/addresses | 用户端 | Customer-Token | customer:self | 60s | 是 | `address.service.spec.ts` 默认事务 + 越权 FORBIDDEN |
| GET /api/v1/admin/customers | 平台 Web | Admin-Token | admin:customers:view | — | 否 | `admin-user.service.spec.ts`(7) |
| GET /api/v1/admin/customers/:id | 平台 Web | Admin-Token | admin:customers:view | — | 否 | `admin-user.service.spec.ts` |
| GET /api/v1/admin/customers/:id/realname-records | 平台 Web | Admin-Token | admin:customers:view | — | 否 | `admin-user.service.spec.ts` |
| POST /api/v1/admin/customers/:id/status | 平台 Web | Admin-Token | admin:customers:disable | 60s | 是 | `admin-user.service.spec.ts` 幂等 + disable 发事件 |

逐接口手动检查:

- [x] 路径与 `接口契约清单.md` 一致 — 见 `apps/customer-app/src/api/index.ts:Endpoints`(stage 1 7 个常量)与 `apps/admin-web/src/api/admin-customers.ts:CustomerEndpoints`。
- [x] HTTP Method 正确 — 全部 POST/GET 与 DESIGN 一致。
- [x] 必填字段 class-validator 校验 — 见 `*.dto.ts` 文件中的 `@IsString` `@Matches` `@Length`。
- [x] 关键值后端权威:realname status / account status / register source 全为 entity ENUM。
- [x] 前端 DTO 字段与后端 VO 严格对齐(均从 `@o2o/contracts` 共享或前端从 service 推断)。
- [x] 后端响应字段全部使用,无未处理字段。
- [x] 分页 pageNo/pageSize 一致;list 返回 total 字段。
- [x] 越权返回 FORBIDDEN — `address.service.spec.ts` "越权访问他人地址"。
- [x] sms-code 接入 IdempotencyInterceptor;每次调用记录写入 `integration_request_log`(`sms.service.spec.ts` happy 验证)。
- [x] 管理端写接口 @Audit — `admin-user.controller.ts` POST :id/status 装饰器。

## 5. 四端联动测试

| 场景 | 用户端 | 商家 APP | 骑手 APP | 平台 Web | 结果 | 证据 |
|---|---|---|---|---|---|---|
| 手机号登录主流程 | 必测 | 不涉及 | 不涉及 | 不涉及 | 自审通过 | `customer-auth.service.spec.ts` happy + e2e 待真机 |
| 自动注册 3 表事务 | 必测 | 不涉及 | 不涉及 | 不涉及 | 自审通过 | profile.createDefault + messageSetting.createDefault 同事务 |
| 后台禁用账号 → 用户登出 | 只消费接口 | 不涉及 | 不涉及 | 必测 | 自审通过 | `admin-user.service` change-status + `customer-account-disabled.subscriber` revoke 设备 |
| 端 Token 隔离 | 必测 | 不涉及 | 不涉及 | 必测 | 通过 | `customer-auth.cross-scope.spec.ts`(7 用例) |
| 实名审核 | 必测 | 不涉及 | 不涉及 | 只消费接口 | 自审通过 | `realname.service.spec.ts` + 平台 Web 详情页查记录 |

### 5.1 端侧适用性

| 端 | 本阶段状态 | 自审记录 |
|---|---|---|
| 用户端(H5/APP/小程序) | 必测 | 11 页全部交付,vitest 21 用例 |
| 商家端 APP | 不涉及 | 阶段 2 起处理;本阶段无商家代码 |
| 骑手端 APP | 不涉及 | 阶段 3 起处理;本阶段无骑手代码 |
| 平台 Web | 必测 | 4 页交付,vitest 8 用例;`v-permission` 控制禁用按钮可见性 |

## 6. 第三方联调

- [x] 地图:本阶段仅占位 — `MapView.vue` 已存在,真接入留 stage 5+。
- [x] 支付:不涉及(stage 4+)。
- [x] 短信:`sms.adapter.ts` mock 模式覆盖 send + integration_request_log;真 SDK stub 抛 `not configured`。
- [x] 推送:不涉及(stage 3+)。
- [x] 实名:`realname.adapter.ts` mock(姓名首字汉字 → success);真 SDK stub。
- [x] 文件存储:本阶段不上传头像/身份证图片(stage 11 接入)。
- [x] 全部 mock 失败行为有标准化文案("内容不符 / 三要素不一致 / 三方不可用")并通过单测验证。
- [x] 真分支均抛 `not configured`,无空实现。

### 6.1 第三方适用性

| 第三方 | 本阶段结论 | 备注 |
|---|---|---|
| 地图(高德) | 不适用 | stage 5+ 接 |
| 支付 | 不适用 | stage 4 接 |
| 短信(阿里) | 必测(mock 模式) | mock 行为覆盖 send + log;real-stub 待人工配凭证后冒烟 |
| 推送(个推) | 不适用 | stage 3 接 |
| 实名(阿里) | 必测(mock 模式) | mock 行为覆盖 verify;real-stub 待人工配凭证 |
| 微信登录 | 必测(mock 模式) | jscode2session 稳定 openId mock |
| 文件存储 | 不适用 | stage 11 接 |

## 7. 问题清单

| 编号 | 级别 | 发现 | 文件/接口 | 问题 | 影响 | 修复 | 状态 |
|---|---|---|---|---|---|---|---|
| 1 | P2 | 2026-05-05 | `packages/contracts` | CJS dist `__exportStar` 让 admin-web vite build 解析命名导入失败 | 生产构建失败 | 增加 ESM 双产物(`tsconfig.esm.json`)+ `package.json` 条件 exports | **已修+复测**(`pnpm --filter @o2o/admin-web build` ✓built in 11.47s) |

P0:0 项 / P1:0 项 / P2:0 项(1 项已修)/ P3:0 项。

## 8. 是否允许进入下一阶段

- [x] P0 已清零。
- [x] P1 已清零。
- [x] 接口审查无阻塞问题(108 后端单测 + 29 前端单测全绿)。
- [x] 四端边界无违规(`scope-jwt.guard` + `cross-scope.spec` 双重保险)。
- [ ] 真机/真接口冒烟:**留待人工**(需启 MySQL 3307 + Redis + `pnpm migrate:run` + 启 server 跑 12 接口 curl)
- [ ] 审查人签字:Claude(自审)
- [ ] 复核人签字:待补
