# 阶段1-用户端-账号地址与基础框架 问题与风险记录

> 商家端仅 Android APP / iOS APP，禁止规划商家 Web 后台、商家小程序。外卖订单与跑腿订单必须独立订单体系、独立状态机、独立计价、独立退款规则。

## P0 阻塞级

- [ ] 暂无。发现后必须填写：文件/接口、问题、影响、修复标准、责任人、复测结果。

## P1 严重级

- [ ] 暂无。发现后必须填写：文件/接口、问题、影响、修复标准、责任人、复测结果。

## P2 重要级

- [x] **[STAGE-0 遗留 → 阶段 1 已修]** admin-web 生产构建失败
  - 文件:`packages/contracts/{package.json,tsconfig.esm.json}`
  - 问题:contracts CJS dist 通过 `__exportStar` 重导出,Rollup 静态分析无法解析命名导入 → `"Header" is not exported`。
  - 修复(T16 前置):新增 `tsconfig.esm.json`(module=ESNext, moduleResolution=Bundler, outDir=dist/esm),package.json 加 dual `exports`(`import`→`./dist/esm/index.js`, `require`→`./dist/index.js`),build script 串联 CJS+ESM 两个 tsc。
  - 复测:`pnpm --filter @o2o/admin-web build` ✓built in 11.47s;`pnpm --filter @o2o/server test --testPathPattern=adapters` 14/14 通过。
  - 复测时间:阶段 1 T16 启动前

## P3 优化级

- [ ] 暂无。发现后必须填写：文件/接口、问题、影响、修复标准、责任人、复测结果。

## 待确认接口问题

| 接口 | 问题 | 阻塞级别 | 决策人 | 结论 |
|---|---|---|---|---|
|  |  |  |  |  |

## 待确认页面问题

| 端 | 页面 | 问题 | 阻塞级别 | 结论 |
|---|---|---|---|---|
|  |  |  |  |  |
